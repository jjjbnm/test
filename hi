<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>רצף | בית תמיכה</title>

<!-- Web App Manifest (JSON) — turns this into an installable app on the phone's home screen -->
<link rel="manifest" href='data:application/manifest+json,{"name":"רצף - בית תמיכה","short_name":"רצף","start_url":".","display":"standalone","background_color":"%23f3f4f9","theme_color":"%233b63e0","lang":"he","dir":"rtl","icons":[{"src":"data:image/svg+xml,%3Csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 100 100%27%3E%3Crect width=%27100%27 height=%27100%27 rx=%2722%27 fill=%27%233b63e0%27/%3E%3Ctext x=%2750%27 y=%2766%27 font-size=%2748%27 text-anchor=%27middle%27 fill=%27white%27 font-family=%27sans-serif%27%3E%D7%A8%3C/text%3E%3C/svg%3E","sizes":"192x192","type":"image/svg+xml"}]}'>
<meta name="theme-color" content="#3b63e0">
<meta name="color-scheme" content="light dark">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="apple-mobile-web-app-title" content="רצף">

<!-- JSON-LD structured data — helps search engines understand this page -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "רצף",
  "description": "בית התמיכה הרשמי של קבוצת רצף ב-TikTok — חוקים, סטטוסים, מנויים ותשלומים.",
  "sameAs": []
}
</script>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;600;700;800;900&family=Assistant:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    color-scheme: light;
    --bg:#f3f4f9;
    --bg2:#ffffff;
    --glass:rgba(255,255,255,0.55);
    --glass-border:rgba(20,23,40,0.09);
    --text:#161a26;
    --muted:#656b80;
    --accent:#3b63e0;
    --accent2:#7c3aed;
    --glass-blur:18px;

    --c-green:#22c55e;
    --c-yellow:#eab308;
    --c-orange:#f97316;
    --c-red:#ef4444;
    --c-black:#6b7280;
    --c-skull:#7c1230;
  }
  html[data-theme="dark"]{
    color-scheme: dark;
    --bg:#0a0c12;
    --bg2:#11141d;
    --glass:rgba(255,255,255,0.045);
    --glass-border:rgba(255,255,255,0.09);
    --text:#eef0f5;
    --muted:#9aa1b4;
  }
  *{box-sizing:border-box; margin:0; padding:0;}
  html{scroll-behavior:smooth;}
  body{
    background:var(--bg);
    color:var(--text);
    font-family:'Assistant', sans-serif;
    line-height:1.6;
    overflow-x:hidden;
    position:relative;
  }
  h1,h2,h3,.display{font-family:'Rubik', sans-serif;}

  /* ambient blobs */
  .blob{position:fixed; border-radius:50%; filter:blur(100px); opacity:0.28; z-index:0; pointer-events:none;}
  .blob1{width:520px; height:520px; background:radial-gradient(circle, var(--accent), transparent 70%); top:-180px; right:-120px; animation:float1 22s ease-in-out infinite;}
  .blob2{width:460px; height:460px; background:radial-gradient(circle, var(--accent2), transparent 70%); bottom:-160px; left:-140px; animation:float2 26s ease-in-out infinite;}
  .blob3{width:300px; height:300px; background:radial-gradient(circle, var(--c-red), transparent 70%); top:45%; left:40%; opacity:0.09; animation:float1 30s ease-in-out infinite reverse;}
  @keyframes float1{0%,100%{transform:translate(0,0);} 50%{transform:translate(-40px,50px);}}
  @keyframes float2{0%,100%{transform:translate(0,0);} 50%{transform:translate(50px,-40px);}}

  .wrap{position:relative; z-index:1; max-width:920px; margin:0 auto; padding:0 20px;}

  /* glass card base */
  .glass{
    background:var(--glass);
    border:1px solid var(--glass-border);
    backdrop-filter:blur(var(--glass-blur));
    -webkit-backdrop-filter:blur(var(--glass-blur));
    border-radius:20px;
    box-shadow:0 8px 30px rgba(20,23,40,0.06);
  }

  /* header */
  header{padding:60px 0 40px; text-align:center;}
  .badge{
    display:inline-flex; align-items:center; gap:8px;
    padding:6px 16px; border-radius:999px; font-size:13px; color:var(--muted);
    margin-bottom:20px;
  }
  .badge span.dot{width:7px; height:7px; border-radius:50%; background:var(--c-green); box-shadow:0 0 8px var(--c-green);}
  header h1{
    font-size:clamp(38px,7vw,64px); font-weight:900;
    background:linear-gradient(90deg,#161a26,var(--accent) 60%, var(--accent2));
    -webkit-background-clip:text; background-clip:text; color:transparent;
    letter-spacing:-0.02em;
  }
  header p{color:var(--muted); font-size:17px; max-width:520px; margin:14px auto 0;}

  .cta-row{display:flex; gap:14px; justify-content:center; margin-top:32px; flex-wrap:wrap;}
  .btn{
    display:inline-flex; align-items:center; gap:10px;
    padding:14px 26px; border-radius:14px; font-weight:700; font-size:15px;
    text-decoration:none; color:var(--text); border:1px solid var(--glass-border);
    transition:transform .18s ease, box-shadow .18s ease, background .18s ease;
    cursor:pointer;
    backdrop-filter:blur(var(--glass-blur));
    -webkit-backdrop-filter:blur(var(--glass-blur));
  }
  .btn:hover{transform:translateY(-2px);}
  .btn-primary{background:linear-gradient(135deg,var(--accent),var(--accent2)); border:none; box-shadow:0 8px 24px rgba(91,140,255,0.28);}
  .btn-primary:hover{box-shadow:0 12px 30px rgba(139,92,246,0.35);}
  .btn-ghost{background:var(--glass);}
  .btn-tiktok{background:rgba(0,0,0,0.9); border:1px solid #000; color:#fff;}
  .btn svg{width:18px; height:18px;}

  section{padding:50px 0;}
  .eyebrow{color:var(--accent); font-size:13px; font-weight:700; letter-spacing:.08em; text-transform:uppercase; margin-bottom:10px; display:block;}
  .sec-title{font-size:clamp(24px,4vw,32px); font-weight:800; margin-bottom:8px;}
  .sec-sub{color:var(--muted); margin-bottom:32px; font-size:15px;}

  /* tabs */
  .tabs-nav{
    position:sticky; top:14px; z-index:5;
    display:flex; gap:6px; justify-content:center; flex-wrap:wrap;
    padding:6px; margin:10px auto 0; width:fit-content; max-width:100%;
  }
  .tab-btn{
    border:none; background:transparent; color:var(--muted);
    font-family:'Assistant', sans-serif; font-weight:600; font-size:14px;
    padding:10px 18px; border-radius:14px; cursor:pointer;
    transition:background .18s ease, color .18s ease;
    white-space:nowrap;
  }
  .tab-btn:hover{color:var(--text);}
  .tab-btn.active{
    background:linear-gradient(135deg,var(--accent),var(--accent2));
    color:#fff; box-shadow:0 6px 18px rgba(91,140,255,0.28);
  }
  .tab-panel{display:none;}
  .tab-panel.active{display:block; animation:fadeIn .35s ease;}
  @keyframes fadeIn{from{opacity:0; transform:translateY(8px);} to{opacity:1; transform:translateY(0);}}

  /* status ladder — signature element */
  .ladder{position:relative; padding-inline-start:0;}
  .ladder::before{
    content:''; position:absolute; top:10px; bottom:10px; right:27px;
    width:2px; background:linear-gradient(180deg, var(--c-green), var(--c-yellow), var(--c-orange), var(--c-red), var(--c-black), var(--c-skull));
    opacity:0.5;
  }
  .rung{
    position:relative; display:flex; align-items:flex-start; gap:18px;
    padding:18px 20px; margin-bottom:12px; padding-inline-end:60px;
  }
  .rung .node{
    position:relative; z-index:1; flex-shrink:0;
    width:56px; height:56px; border-radius:50%;
    display:flex; align-items:center; justify-content:center;
    font-size:24px; border:2px solid rgba(20,23,40,0.1);
  }
  .rung .body h3{font-size:17px; font-weight:700; margin-bottom:4px;}
  .rung .body p{color:var(--muted); font-size:14px;}
  .rung[data-lvl="1"] .node{box-shadow:0 0 22px rgba(34,197,94,.5); background:rgba(34,197,94,.12);}
  .rung[data-lvl="2"] .node{box-shadow:0 0 22px rgba(234,179,8,.5); background:rgba(234,179,8,.12);}
  .rung[data-lvl="3"] .node{box-shadow:0 0 22px rgba(249,115,22,.5); background:rgba(249,115,22,.12);}
  .rung[data-lvl="4"] .node{box-shadow:0 0 22px rgba(239,68,68,.5); background:rgba(239,68,68,.12);}
  .rung[data-lvl="5"] .node{box-shadow:0 0 22px rgba(107,114,128,.5); background:rgba(107,114,128,.16);}
  .rung[data-lvl="6"] .node{box-shadow:0 0 26px rgba(124,18,48,.65); background:rgba(124,18,48,.22);}

  /* rules */
  .rules-card{padding:30px;}
  .rules-card ol{list-style:none; counter-reset:rule;}
  .rules-card li{
    counter-increment:rule; position:relative; padding-inline-start:44px; padding-block:12px;
    border-bottom:1px solid rgba(255,255,255,0.06); font-size:15px;
  }
  .rules-card li:last-child{border-bottom:none;}
  .rules-card li::before{
    content:counter(rule); position:absolute; inset-inline-start:0; top:10px;
    width:28px; height:28px; border-radius:8px; background:rgba(91,140,255,.15); color:var(--accent);
    display:flex; align-items:center; justify-content:center; font-weight:800; font-size:13px; font-family:'Rubik',sans-serif;
  }
  .rules-note{margin-top:18px; padding:14px 16px; border-radius:12px; background:rgba(239,68,68,.08); border:1px solid rgba(239,68,68,.25); font-size:13.5px; color:#b91c1c;}

  /* effective date strip */
  .date-strip{
    display:flex; align-items:center; gap:14px; padding:16px 22px; margin-bottom:28px;
    border-inline-start:3px solid var(--accent2);
  }
  .date-strip .big{font-family:'Rubik'; font-weight:800; font-size:18px;}
  .date-strip .small{color:var(--muted); font-size:13.5px;}

  /* subscriptions */
  .plans{display:grid; grid-template-columns:repeat(auto-fit,minmax(230px,1fr)); gap:16px;}
  .plan{padding:26px 22px; display:flex; flex-direction:column; gap:10px; position:relative; overflow:hidden;}
  .plan.highlight{border-color:rgba(139,92,246,0.4);}
  .plan.highlight::before{content:'הכי פופולרי'; position:absolute; top:14px; left:-32px; transform:rotate(-40deg); background:var(--accent2); color:#fff; font-size:11px; font-weight:700; padding:3px 40px;}
  .plan .name{font-size:14px; color:var(--muted); font-weight:600;}
  .plan .price{font-family:'Rubik'; font-size:32px; font-weight:900;}
  .plan .price span{font-size:14px; color:var(--muted); font-weight:500;}
  .plan ul{margin-top:8px; display:flex; flex-direction:column; gap:8px;}
  .plan li{font-size:13.5px; color:#c7cbe0; display:flex; gap:8px; align-items:flex-start;}
  .plan li::before{content:'✓'; color:var(--c-green); font-weight:800; flex-shrink:0;}

  .plan ul.plusminus li::before{content:none;}
  .plan ul.plusminus li.yes{color:#15803d; font-weight:700;}
  .plan ul.plusminus li.no{color:#b91c1c;}

  /* shop */
  .shop-grid{display:grid; grid-template-columns:repeat(auto-fit,minmax(210px,1fr)); gap:16px;}
  .shop-item{padding:22px 20px; display:flex; flex-direction:column; gap:6px;}
  .shop-item .name{font-weight:700; font-size:15px;}
  .shop-item .price{font-family:'Rubik'; font-weight:900; font-size:24px; color:var(--accent);}
  .shop-item .price.free{color:#15803d;}
  .shop-item .note{font-size:12px; color:var(--muted);}
  .shop-item .note.warn{color:#b91c1c;}

  /* settings */
  .settings-card{padding:24px 22px; margin-bottom:16px;}
  .settings-card h3{font-size:15px; margin-bottom:14px;}
  .swatch-row{display:flex; gap:12px; flex-wrap:wrap;}
  .swatch{
    width:44px; height:44px; border-radius:50%; cursor:pointer; border:3px solid transparent;
    transition:transform .15s ease, border-color .15s ease;
  }
  .swatch:hover{transform:scale(1.08);}
  .swatch.active{border-color:var(--text);}
  .toggle-row{display:flex; align-items:center; justify-content:space-between;}
  .toggle-switch{
    width:52px; height:30px; border-radius:999px; background:var(--glass-border);
    position:relative; cursor:pointer; transition:background .2s ease; border:none;
  }
  .toggle-switch::after{
    content:''; position:absolute; top:3px; right:3px; width:24px; height:24px; border-radius:50%;
    background:#fff; transition:right .2s ease; box-shadow:0 2px 6px rgba(0,0,0,0.2);
  }
  .toggle-switch.on{background:linear-gradient(135deg,var(--accent),var(--accent2));}
  .toggle-switch.on::after{right:25px;}
  .blur-slider{width:100%; margin-top:10px; accent-color:var(--accent);}
  .lang-row{display:flex; gap:10px;}
  .lang-btn{
    flex:1; padding:10px; border-radius:12px; border:1px solid var(--glass-border);
    background:var(--bg); color:var(--muted); font-family:'Assistant'; font-weight:700; cursor:pointer;
  }
  .lang-btn.active{background:linear-gradient(135deg,var(--accent),var(--accent2)); color:#fff; border:none;}
  .buy-btn{
    margin-top:8px; padding:9px; border-radius:10px; border:none; cursor:pointer;
    font-family:'Assistant'; font-weight:700; font-size:13px; color:#fff;
    background:linear-gradient(135deg,var(--accent),var(--accent2));
  }
  .plan .tag{font-size:11.5px; padding:4px 10px; border-radius:999px; background:rgba(255,255,255,.06); width:fit-content; color:var(--muted);}

  .cap-list{display:flex; flex-direction:column; gap:7px; margin-top:6px;}
  .cap{display:flex; align-items:center; gap:9px; font-size:13.5px;}
  .cap .mark{width:19px; height:19px; border-radius:6px; display:flex; align-items:center; justify-content:center; font-weight:800; font-size:13px; flex-shrink:0;}
  .cap.yes{color:var(--text);}
  .cap.yes .mark{background:rgba(34,197,94,.15); color:#15803d;}
  .cap.no{color:var(--muted); text-decoration:line-through;}
  .cap.no .mark{background:rgba(239,68,68,.12); color:#b91c1c;}

  .price-tiers{margin-top:14px; display:flex; flex-direction:column; gap:0; border-top:1px solid var(--glass-border);}
  .price-tiers .tier{display:flex; justify-content:space-between; padding:8px 0; font-size:13px; border-bottom:1px solid var(--glass-border);}
  .price-tiers .tier span:first-child{color:var(--muted);}
  .price-tiers .tier span:last-child{font-weight:700; font-family:'Rubik';}
  .price-tiers button.tier{all:unset; display:flex; justify-content:space-between; padding:8px 0; font-size:13px; border-bottom:1px solid var(--glass-border); width:100%; cursor:pointer; box-sizing:border-box; font-family:'Assistant';}
  .price-tiers button.tier:hover{background:rgba(91,140,255,.08); border-radius:8px; padding-inline:6px;}

  /* removed vs new */
  .diff-grid{display:grid; grid-template-columns:1fr 1fr; gap:16px;}
  @media(max-width:640px){.diff-grid{grid-template-columns:1fr;}}
  .diff-card{padding:22px;}
  .diff-card h3{font-size:14px; margin-bottom:12px; display:flex; align-items:center; gap:8px;}
  .diff-card.removed h3{color:#b91c1c;}
  .diff-card.added h3{color:#15803d;}
  .diff-card p{font-size:13.5px; color:var(--muted);}

  /* age / geo notice */
  .notice-grid{display:grid; grid-template-columns:1fr 1fr; gap:16px;}
  @media(max-width:640px){.notice-grid{grid-template-columns:1fr;}}
  .notice{padding:22px;}
  .notice .k{font-family:'Rubik'; font-weight:800; font-size:22px; margin-bottom:4px;}
  .notice .v{color:var(--muted); font-size:13.5px;}

  /* status check widget */
  .checker{padding:28px; text-align:center;}
  .checker select{
    background:var(--bg2); color:var(--text); border:1px solid var(--glass-border);
    padding:12px 16px; border-radius:12px; font-size:15px; font-family:'Assistant'; width:100%; max-width:280px; margin:16px auto 0; display:block;
  }
  .result{margin-top:18px; padding:14px; border-radius:12px; font-weight:700; display:none;}

  /* footer / contact */
  footer{padding:60px 0 50px; text-align:center;}
  .contact-card{padding:36px 24px;}
  .contact-card h2{font-size:22px; margin-bottom:8px;}
  .contact-card p{color:var(--muted); font-size:14px; margin-bottom:24px;}

  .fine{color:var(--muted); font-size:12.5px; margin-top:30px; text-align:center; padding-bottom:20px;}

  /* profile avatar — top corner */
  .profile-avatar{
    position:fixed; top:18px; left:18px; z-index:10;
    width:52px; height:52px; border-radius:50%;
    overflow:hidden; display:flex; align-items:center; justify-content:center;
    box-shadow:0 6px 20px rgba(20,23,40,0.12);
  }
  .profile-avatar img{width:100%; height:100%; object-fit:cover; display:none;}
  .profile-avatar .placeholder{
    width:100%; height:100%; display:flex; align-items:center; justify-content:center;
    color:var(--muted);
  }
  .profile-avatar .placeholder svg{width:26px; height:26px;}
  .profile-avatar.logged-in .placeholder{display:none;}
  .profile-avatar.logged-in img{display:block;}

  @media(max-width:600px){
    .rung{padding-inline-end:20px;}
    .profile-avatar{width:42px; height:42px; top:12px; left:12px;}
  }

  /* AI assistant widget */
  .ai-fab{
    position:fixed; bottom:22px; left:22px; z-index:20;
    width:56px; height:56px; border-radius:50%; border:none; cursor:pointer;
    background:linear-gradient(135deg,var(--accent),var(--accent2));
    color:#fff; font-size:24px;
    box-shadow:0 10px 28px rgba(91,140,255,0.35);
    display:flex; align-items:center; justify-content:center;
    transition:transform .18s ease;
  }
  .ai-fab:hover{transform:scale(1.06);}

  /* shopping cart */
  .cart-fab{
    position:fixed; bottom:22px; right:22px; z-index:20;
    width:56px; height:56px; border-radius:50%; border:none; cursor:pointer;
    background:var(--bg2); border:1px solid var(--glass-border);
    color:var(--text); font-size:22px;
    box-shadow:0 10px 28px rgba(20,23,40,0.18);
    display:flex; align-items:center; justify-content:center;
    transition:transform .18s ease;
  }
  .cart-fab:hover{transform:scale(1.06);}
  .cart-badge{
    position:absolute; top:-4px; left:-4px; min-width:20px; height:20px; border-radius:999px;
    background:linear-gradient(135deg,var(--accent),var(--accent2)); color:#fff;
    font-size:11px; font-weight:800; display:flex; align-items:center; justify-content:center;
    padding:0 4px; display:none;
  }
  .cart-badge.show{display:flex;}
  .cart-panel{
    position:fixed; bottom:88px; right:22px; z-index:20;
    width:min(340px, calc(100vw - 44px)); max-height:min(480px, 70vh);
    display:none; flex-direction:column; overflow:hidden;
    background:var(--bg2); border:1px solid var(--glass-border); border-radius:20px;
    box-shadow:0 20px 60px rgba(20,23,40,0.25);
  }
  .cart-panel.open{display:flex;}
  .cart-panel-header{
    padding:14px 16px; border-bottom:1px solid var(--glass-border);
    display:flex; align-items:center; justify-content:space-between; font-weight:700; font-size:14px;
  }
  .cart-panel-header button{background:none; border:none; color:var(--muted); font-size:18px; cursor:pointer;}
  .cart-items{flex:1; overflow-y:auto; padding:12px 16px; display:flex; flex-direction:column; gap:8px;}
  .cart-item{display:flex; align-items:center; justify-content:space-between; gap:8px; font-size:13px; padding:8px 0; border-bottom:1px solid var(--glass-border);}
  .cart-item .remove-item{background:none; border:none; color:#b91c1c; cursor:pointer; font-size:16px; flex-shrink:0;}
  .cart-empty{padding:24px; text-align:center; color:var(--muted); font-size:13px;}
  .cart-footer{padding:14px 16px; border-top:1px solid var(--glass-border); display:flex; flex-direction:column; gap:10px;}
  .cart-total{display:flex; justify-content:space-between; font-weight:800; font-family:'Rubik'; font-size:16px;}
  .add-to-cart-btn{
    display:flex; align-items:center; gap:6px;
    border-radius:10px; border:1px solid var(--glass-border); background:var(--glass);
    color:var(--text); cursor:pointer; font-family:'Assistant'; font-weight:600;
  }
  .subscribe-btn{
    display:flex; align-items:center; justify-content:center; gap:6px;
    border-radius:10px; border:1px solid var(--accent); background:transparent;
    color:var(--accent); cursor:pointer; font-family:'Assistant'; font-weight:700;
  }

  .ai-panel{
    position:fixed; bottom:88px; left:22px; z-index:20;
    width:min(340px, calc(100vw - 44px)); max-height:min(480px, 70vh);
    display:none; flex-direction:column; overflow:hidden;
    background:var(--bg2); border:1px solid var(--glass-border); border-radius:20px;
    box-shadow:0 20px 60px rgba(20,23,40,0.25);
  }
  .ai-panel.open{display:flex;}
  .ai-panel-header{
    padding:14px 16px; border-bottom:1px solid var(--glass-border);
    display:flex; align-items:center; justify-content:space-between; font-weight:700; font-size:14px;
  }
  .ai-panel-header button{background:none; border:none; color:var(--muted); font-size:18px; cursor:pointer;}
  .ai-messages{flex:1; overflow-y:auto; padding:14px 16px; display:flex; flex-direction:column; gap:10px; font-size:13.5px;}
  .ai-msg{padding:9px 13px; border-radius:12px; max-width:88%; line-height:1.5;}
  .ai-msg.user{align-self:flex-end; background:linear-gradient(135deg,var(--accent),var(--accent2)); color:#fff;}
  .ai-msg.bot{align-self:flex-start; background:rgba(120,120,140,0.1); color:var(--text);}
  .ai-msg.bot.loading{color:var(--muted); font-style:italic;}
  .ai-input-row{display:flex; gap:8px; padding:12px; border-top:1px solid var(--glass-border);}
  .ai-input-row input{
    flex:1; padding:10px 12px; border-radius:12px; border:1px solid var(--glass-border);
    background:var(--bg); color:var(--text); font-family:'Assistant'; font-size:13.5px;
  }
  .ai-input-row button{
    padding:10px 16px; border-radius:12px; border:none; cursor:pointer; font-weight:700; font-size:13.5px;
    background:linear-gradient(135deg,var(--accent),var(--accent2)); color:#fff;
  }

  /* consent modal — this is the SITE asking before sending the user to TikTok's own screen */
  .modal-overlay{
    position:fixed; inset:0; background:rgba(10,12,18,0.45);
    display:none; align-items:center; justify-content:center; z-index:50; padding:20px;
  }
  .modal-overlay.open{display:flex;}
  .modal-card{
    position:relative;
    max-width:360px; width:100%; padding:28px 24px; text-align:center;
    background:var(--bg2); border:1px solid var(--glass-border); border-radius:20px;
    box-shadow:0 20px 60px rgba(20,23,40,0.25);
  }
  .modal-card .modal-icon{
    width:52px; height:52px; border-radius:14px; margin:0 auto 16px;
    background:linear-gradient(135deg,var(--accent),var(--accent2));
    display:flex; align-items:center; justify-content:center; color:#fff;
  }
  .modal-card h3{font-size:17px; margin-bottom:10px;}
  .modal-card p{font-size:14px; color:var(--muted); margin-bottom:22px;}
  .modal-actions{display:flex; gap:10px;}
  .modal-actions .btn{flex:1; justify-content:center; padding:12px;}
</style>
<!-- TypeScript compiler (runs in-browser to transpile the typed code below) -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/typescript/5.9.2/typescript.min.js"></script>
</head>
<body>

<script>
  // מציג כל שגיאת JS ישירות על המסך — כדי לאתר תקלות מהטלפון, בלי מחשב/devtools
  window.addEventListener('error', function(e){
    var box = document.createElement('div');
    box.style.cssText = 'position:fixed;top:0;left:0;right:0;z-index:999999;background:#b91c1c;color:#fff;padding:14px;font-size:13px;direction:ltr;text-align:left;white-space:pre-wrap;font-family:monospace;';
    box.textContent = 'JS ERROR: ' + e.message + '\n@ ' + e.filename + ':' + e.lineno + ':' + e.colno;
    document.body.insertBefore(box, document.body.firstChild);
  });
  window.addEventListener('unhandledrejection', function(e){
    var box = document.createElement('div');
    box.style.cssText = 'position:fixed;top:0;left:0;right:0;z-index:999999;background:#b91c1c;color:#fff;padding:14px;font-size:13px;direction:ltr;text-align:left;white-space:pre-wrap;font-family:monospace;';
    box.textContent = 'PROMISE ERROR: ' + (e.reason && e.reason.message ? e.reason.message : e.reason);
    document.body.insertBefore(box, document.body.firstChild);
  });
</script>

<div class="profile-avatar glass" id="profileAvatar" title="התחברות עם TikTok" style="cursor:pointer;">
  <img id="profileAvatarImg" src="" alt="תמונת פרופיל TikTok">
  <span class="placeholder">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="12" cy="8" r="3.5"/><path d="M4.5 20c1.5-4 5-6 7.5-6s6 2 7.5 6"/></svg>
  </span>
</div>

<div class="modal-overlay" id="consentModal">
  <div class="modal-card">
    <div class="modal-icon">
      <svg viewBox="0 0 24 24" fill="currentColor" width="24" height="24"><path d="M16.6 5.82s.51.5 0 0A4.278 4.278 0 0 1 15.54 3h-3.09v12.4a2.592 2.592 0 0 1-2.59 2.5c-1.42 0-2.6-1.16-2.6-2.6 0-1.72 1.66-3.01 3.37-2.48V9.66c-3.45-.46-6.47 2.22-6.47 5.64 0 3.33 2.76 5.7 5.69 5.7 3.14 0 5.69-2.55 5.69-5.7V9.01a7.35 7.35 0 0 0 4.3 1.38V7.3s-1.88.09-3.24-1.48z"/></svg>
    </div>
    <h3>האתר מבקש גישה</h3>
    <p>לתמונת הפרופיל והשם שלך ב-TikTok, ולבדוק אם אתה חבר רגיל, מנהל או בעלים בקבוצה.</p>
    <div class="modal-actions">
      <button type="button" id="consentYes" class="btn btn-primary">כן</button>
      <button type="button" id="consentNo" class="btn btn-ghost glass">לא</button>
    </div>
  </div>
</div>

<div class="modal-overlay" id="usernameModal" style="z-index:60;">
  <div class="modal-card">
    <div class="modal-icon">👤</div>
    <h3>ברוכים הבאים לרצף</h3>
    <p>איך קוראים לכם? זה יישמר במכשיר הזה בלבד.</p>
    <input type="text" id="usernameInput" placeholder="שם משתמש" style="width:100%; padding:12px; border-radius:12px; border:1px solid var(--glass-border); background:var(--bg); color:var(--text); font-family:'Assistant'; font-size:15px; margin-bottom:14px; text-align:center;">
    <div class="modal-actions">
      <button type="button" id="usernameSubmit" class="btn btn-primary" style="flex:1; justify-content:center;">כניסה</button>
    </div>
  </div>
</div>

<div class="modal-overlay" id="paypalModal">
  <div class="modal-card" style="text-align:right;">
    <button type="button" id="paypalModalClose" style="position:absolute; top:14px; left:14px; background:none; border:none; color:var(--muted); font-size:18px; cursor:pointer;">✕</button>
    <div style="text-align:center;">
      <h3 id="paypalItemName" style="margin-bottom:2px;"></h3>
      <p id="paypalItemPrice" style="font-family:'Rubik'; font-weight:800; font-size:22px; color:var(--accent); margin-bottom:18px;"></p>
    </div>
    <div id="paypalButtonContainer"></div>
    <div id="applePayButtonContainer" style="margin-top:10px;"></div>
    <div id="googlePayButtonContainer" style="margin-top:10px;"></div>
    <p style="font-size:11.5px; color:var(--muted); margin-top:14px; text-align:center;">התשלום מאובטח דרך PayPal.</p>
  </div>
</div>

<div class="modal-overlay" id="subModal">
  <div class="modal-card" style="text-align:right;">
    <button type="button" id="subModalClose" style="position:absolute; top:14px; left:14px; background:none; border:none; color:var(--muted); font-size:18px; cursor:pointer;">✕</button>
    <div style="text-align:center;">
      <h3 id="subItemName" style="margin-bottom:2px;"></h3>
      <p style="font-size:12px; color:var(--muted); margin-bottom:18px;">מנוי מתחדש — חיוב אוטומטי כל תקופה, עד ביטול</p>
    </div>
    <div id="subButtonContainer"></div>
    <p style="font-size:11.5px; color:var(--muted); margin-top:14px; text-align:center;">התשלום מאובטח דרך PayPal. אפשר לבטל בכל עת דרך PayPal.</p>
  </div>
</div>

<div class="blob blob1"></div>
<div class="blob blob2"></div>
<div class="blob blob3"></div>

<div class="wrap">

  <header>
    <div class="badge glass"><span class="dot"></span> <span data-i18n="hero_badge">קהילת רצף · TikTok</span></div>
    <p style="margin-top:8px; font-size:13px; color:var(--muted);">👑 <span data-i18n="hero_owner_label">בעלים</span>: <b style="color:var(--text);">הבאן המקורי</b></p>
    <p id="userGreeting" style="margin-top:4px; font-size:13px; color:var(--muted); display:none;"></p>
    <h1>רצף</h1>
    <p data-i18n="hero_subtitle">בית התמיכה הרשמי של הקבוצה — חוקים, סטטוסים, מנויים והתחברות, במקום אחד.</p>
    <div class="badge glass" style="margin-top:16px; font-weight:700;">🔥 <span data-i18n="hero_streak_label">רצף</span>: <span id="streakDays">335</span> <span data-i18n="hero_days_label">ימים</span></div>
    <div class="cta-row">
      <button type="button" id="tiktokLoginBtn" class="btn btn-tiktok">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M16.6 5.82s.51.5 0 0A4.278 4.278 0 0 1 15.54 3h-3.09v12.4a2.592 2.592 0 0 1-2.59 2.5c-1.42 0-2.6-1.16-2.6-2.6 0-1.72 1.66-3.01 3.37-2.48V9.66c-3.45-.46-6.47 2.22-6.47 5.64 0 3.33 2.76 5.7 5.69 5.7 3.14 0 5.69-2.55 5.69-5.7V9.01a7.35 7.35 0 0 0 4.3 1.38V7.3s-1.88.09-3.24-1.48z"/></svg>
        <span data-i18n="hero_login_btn">התחברות עם TikTok</span>
      </button>
      <a href="#" data-target="contact" class="btn btn-ghost glass tab-jump" data-i18n="hero_tiktok_page_btn">עמוד TikTok של רצף</a>
      <a href="#" data-target="status" class="btn btn-ghost glass tab-jump" data-i18n="hero_check_status_btn">בדיקת סטטוס</a>
    </div>
    <p id="tiktokAuthStatus" style="margin-top:14px; font-size:13.5px; color:var(--muted); display:none;"></p>
    <p style="margin-top:10px; font-size:12.5px; color:var(--muted); max-width:420px; margin-inline:auto;">
      ההתחברות מבקשת רק את השם הציבורי שלכם ב-TikTok, כדי להציג "מחובר בתור @שם".
      אין לה גישה לפרסום, הודעות, עוקבים או כל פעולה בחשבון שלכם.
    </p>
  </header>

  <nav class="tabs-nav glass">
    <button class="tab-btn active" data-target="home" data-i18n="nav_home">בית</button>
    <button class="tab-btn" data-target="status" data-i18n="nav_status">סטטוס</button>
    <button class="tab-btn" data-target="rules" data-i18n="nav_rules">חוקים</button>
    <button class="tab-btn" data-target="subs" data-i18n="nav_subs">מנויים</button>
    <button class="tab-btn" data-target="team" data-i18n="nav_team">בעלים ומנהלים</button>
    <button class="tab-btn" data-target="scope" data-i18n="nav_scope">גיל ותנאים</button>
    <button class="tab-btn" data-target="shop" data-i18n="nav_shop">חנות</button>
    <button class="tab-btn" data-target="settings" data-i18n="nav_settings">הגדרות</button>
    <button class="tab-btn" data-target="about" data-i18n="nav_about">אודות וגרסה</button>
    <button class="tab-btn" data-target="contact" data-i18n="nav_contact">יצירת קשר</button>
  </nav>

  <!-- HOME -->
  <section id="home" class="tab-panel active">
    <span class="eyebrow" data-i18n="home_eyebrow">ברוכים הבאים</span>
    <h2 class="sec-title" data-i18n="home_title">בית</h2>
    <p class="sec-sub" data-i18n="home_sub">כל מה שצריך לדעת על רצף, במקום אחד.</p>

    <div class="shop-grid" style="margin-bottom:20px;">
      <div class="shop-item glass tab-jump" data-target="status" style="cursor:pointer;">
        <span class="name">🟢 סטטוס</span>
        <span class="note">בדקו איפה אתם עומדים בקבוצה</span>
      </div>
      <div class="shop-item glass tab-jump" data-target="rules" style="cursor:pointer;">
        <span class="name">📜 חוקים</span>
        <span class="note">כל חוקי הקבוצה</span>
      </div>
      <div class="shop-item glass tab-jump" data-target="subs" style="cursor:pointer;">
        <span class="name">💳 מנויים</span>
        <span class="note">רצף פלוס, מנוי סרטון ועוד</span>
      </div>
      <div class="shop-item glass tab-jump" data-target="shop" style="cursor:pointer;">
        <span class="name">🛍️ חנות</span>
        <span class="note">תגים, שינויים ועוד</span>
      </div>
    </div>

    <div class="settings-card glass">
      <h3>🆘 מצאתם בעיה?</h3>
      <p style="font-size:13px; color:var(--muted); margin-bottom:14px;">אם משהו לא עובד באתר או בקבוצה, אפשר לפנות ישירות.</p>
      <a href="https://www.tiktok.com/@user613987579196?_r=1&_t=ZS-99MugNRg5zk" target="_blank" class="btn btn-primary" style="width:100%; justify-content:center;">✉️ עלי לפנות לתמיכה</a>
    </div>
  </section>

  <!-- STATUS LADDER -->
  <section id="status" class="tab-panel">
    <span class="eyebrow" data-i18n="status_eyebrow">מערכת סטטוס</span>
    <h2 class="sec-title" data-i18n="status_title">איפה אתה עומד בקבוצה</h2>
    <p class="sec-sub" data-i18n="status_sub">כל חבר מתחיל ירוק. חריגה מהחוקים מעלה אותך בסולם — הסולם לא מתאפס לבד, מנהל מטפל בכל שלב.</p>

    <div class="ladder">
      <div class="rung glass" data-lvl="1">
        <div class="node">🟢</div>
        <div class="body"><h3>הכול טוב</h3><p>אין בעיה. אתה בסדר גמור בקבוצה.</p></div>
      </div>
      <div class="rung glass" data-lvl="2">
        <div class="node">🟡</div>
        <div class="body"><h3>סבבה — אזהרה ראשונה ⚠️</h3><p>חריגה קלה. זו הפעמון הראשון — שימו לב לחוקים.</p></div>
      </div>
      <div class="rung glass" data-lvl="3">
        <div class="node">🟠</div>
        <div class="body"><h3>בסיכון — אזהרה שנייה</h3><p>חריגה חוזרת. הסטטוס הבא הוא כבר סכנה של ממש.</p></div>
      </div>
      <div class="rung glass" data-lvl="4">
        <div class="node">🔴</div>
        <div class="body"><h3>בסכנה — אזהרה חמורה</h3><p>אתה קרוב מאוד לבאן. כל חריגה נוספת עלולה להוציא אותך.</p></div>
      </div>
      <div class="rung glass" data-lvl="5">
        <div class="node">⚫</div>
        <div class="body"><h3>באן</h3><p>חסימה מהקבוצה.</p></div>
      </div>
      <div class="rung glass" data-lvl="6">
        <div class="node">☠️</div>
        <div class="body"><h3>חסימה לצמיתות</h3><p>הרמה החמורה ביותר — אין חזרה.</p></div>
      </div>
    </div>

    <button type="button" id="bannedToggleBtn" class="btn btn-ghost glass" style="margin-top:24px;">
      📋 מי בבאן
    </button>

    <div id="bannedList" class="glass" style="display:none; margin-top:16px; padding:20px 22px;">
      <h3 style="font-size:15px; margin-bottom:12px; color:#b91c1c;">רשימת חסומים נוכחית</h3>
      <ul id="bannedNames" style="list-style:none; display:flex; flex-direction:column; gap:8px; font-size:14px;"></ul>
      <p id="bannedEmpty" style="color:var(--muted); font-size:13.5px; display:none;">אין כרגע אף אחד ברשימה.</p>
    </div>
  </section>

  <!-- RULES -->
  <section id="rules" class="tab-panel">
    <span class="eyebrow" data-i18n="rules_eyebrow">חוקי הקבוצה</span>
    <h2 class="sec-title" data-i18n="rules_title">חוקים</h2>
    <p class="sec-sub" data-i18n="rules_sub">שלושה חוקי בסיס שחלים על כולם, כל הזמן.</p>


    <div class="rules-card glass">
      <ol>
        <li>לא לספים.</li>
        <li>לא לקלל את היוצר, המנהלים, או כל חבר אחר בקבוצה.</li>
        <li>אסור לשנות את השם או את התמונה של הקבוצה.</li>
      </ol>
      <div class="rules-note">
        קללה כלפי מנהל יכולה להוריד ישר לסטטוס סיכון, סכנה או באן — תלוי אם יש חרטה. אם מישהו מקלל בפרטי אבל הוא בקבוצה, המנהל שנפגע צריך להעביר הוכחות; אם זה מאומת, המקלל מקבל באן.
      </div>
      <div class="rules-note">
        אסור לשלוח או להזכיר תוכן שקשור לעזה, פלסטין, לבנון, איראן או עיראק. זה לא כולל רוסיה, סין או צפון קוריאה.
      </div>
    </div>
  </section>

  <div class="tab-panel" id="subs">
  <!-- SUBSCRIPTION CHANGE -->
  <section id="subs-change">
    <span class="eyebrow" data-i18n="subschange_eyebrow">עדכון מנויים</span>
    <h2 class="sec-title" data-i18n="subschange_title">מה משתנה</h2>

    <div class="date-strip glass">
      <div class="big">עדכון מדיניות סרטונים</div>
      <div class="small">שליחת סרטונים דורשת מנוי. מחיר המנוי הבסיסי עולה בשנת 2027.</div>
    </div>

    <div class="diff-grid">
      <div class="diff-card glass removed">
        <h3>❌ מה נמחק</h3>
        <p>שליחת סרטונים חופשית במנוי הפלוס הרגיל — כולל לייבים וסרטונים אישיים — לא תהיה זמינה יותר בחינם.</p>
      </div>
      <div class="diff-card glass added">
        <h3>✅ מה חדש</h3>
        <p>שליחת סרטונים ולייבים עוברת למנויים בתשלום. יש שני מנויים חדשים בתשלום ומנוי מנהלים חינמי.</p>
      </div>
    </div>
  </section>

  <div class="settings-card glass" style="margin:20px 0;">
    <h3>🔁 המנוי שלי</h3>
    <p id="mySubStatus" style="font-size:13px; color:var(--muted); margin-bottom:10px;">לא נמצא מנוי פעיל במכשיר הזה.</p>
    <button type="button" id="checkSubStatusBtn" class="btn btn-ghost glass" style="width:100%; justify-content:center; font-size:13px;">🔄 בדוק סטטוס מנוי</button>
  </div>

  <!-- VIDEO POSTING SUBSCRIPTIONS -->
  <section id="video-subs">
    <span class="eyebrow" data-i18n="videosubs_eyebrow">הרשאת פרסום סרטונים</span>
    <h2 class="sec-title" data-i18n="videosubs_title">מנוי סרטון</h2>
    <p class="sec-sub" data-i18n="videosubs_sub">בנפרד ממנויי רצף פלוס — זה המנוי שקובע מה מותר לשלוח בכלל.</p>

    <div class="plans">
      <div class="plan glass">
        <span class="tag">בסיסי</span>
        <div class="name">מנוי סרטון רגיל</div>
        <div class="price">2.5₪<span>/חודש</span></div>
        <ul class="plusminus">
          <li class="no">− אפשר לשלוח לייבים</li>
          <li class="no">− אפשר לשלוח סרטונים של כולם</li>
          <li class="yes">+ אפשר לשלוח סרטונים שלנו</li>
        </ul>
        <button type="button" class="buy-btn" data-item="מנוי סרטון רגיל (חודש)" data-price="2.5" style="margin-top:14px;">🛒 קנה</button>
        <button type="button" class="subscribe-btn" data-item="מנוי סרטון רגיל (חודשי, מתחדש)" data-plan="video_regular_month" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">🔁 הרשמה כמנוי מתחדש</button>
        <button type="button" class="add-to-cart-btn" data-item="מנוי סרטון רגיל (חודש)" data-price="2.5" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
      <div class="plan glass highlight">
        <span class="tag">מורחב</span>
        <div class="name">מנוי סרטון פלוס</div>
        <div class="price">5₪<span>/חודש</span></div>
        <ul class="plusminus">
          <li class="yes">+ אפשר לשלוח לייבים</li>
          <li class="yes">+ אפשר לשלוח סרטונים שלנו</li>
          <li class="yes">+ אפשר לשלוח סרטונים של כולם</li>
        </ul>
        <button type="button" class="buy-btn" data-item="מנוי סרטון פלוס (חודש)" data-price="5" style="margin-top:14px;">🛒 קנה</button>
        <button type="button" class="subscribe-btn" data-item="מנוי סרטון פלוס (חודשי, מתחדש)" data-plan="video_plus_month" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">🔁 הרשמה כמנוי מתחדש</button>
        <button type="button" class="add-to-cart-btn" data-item="מנוי סרטון פלוס (חודש)" data-price="5" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
    </div>

    <h3 style="font-size:15px; margin-top:28px; margin-bottom:12px;">📅 לוח הנחות</h3>
    <div class="glass" style="padding:18px 20px; display:flex; flex-direction:column; gap:10px; font-size:13.5px;">
      <div style="display:flex; justify-content:space-between; border-bottom:1px solid var(--glass-border); padding-bottom:8px;">
        <span>20–25 בינואר</span><span style="font-weight:700; color:#15803d;">חינם</span>
      </div>
      <div style="display:flex; justify-content:space-between; border-bottom:1px solid var(--glass-border); padding-bottom:8px;">
        <span>1 באפריל</span><span style="font-weight:700;">המחיר עולה בשקל נוסף</span>
      </div>
      <div style="display:flex; justify-content:space-between; border-bottom:1px solid var(--glass-border); padding-bottom:8px;">
        <span>1–5 בספטמבר</span><span style="font-weight:700; color:#15803d;">הנחה 50%</span>
      </div>
      <div style="display:flex; justify-content:space-between; border-bottom:1px solid var(--glass-border); padding-bottom:8px;">
        <span>25 בספטמבר</span><span style="font-weight:700; color:#15803d;">הנחה 25%</span>
      </div>
      <div style="display:flex; justify-content:space-between;">
        <span>1–10 בנובמבר</span><span style="font-weight:700; color:#15803d;">הנחה 11%</span>
      </div>
    </div>
  </section>

  <!-- PLANS -->
  <section id="plans">
    <span class="eyebrow" data-i18n="plans_eyebrow">הטבות קהילה</span>
    <h2 class="sec-title" data-i18n="plans_title">מנויי רצף פלוס</h2>
    <p class="sec-sub">מנהלים מקבלים 1₪ הנחה על כל מנוי בתשלום. חברי צוות נבחרים (למשל shirel) מקבלים מנוי בחינם.</p>

    <div class="plans">
      <div class="plan glass">
        <span class="tag">בסיסי</span>
        <div class="name">רצף פלוס 🔥</div>
        <ul>
          <li>מקום נוסף אחד לספאם</li>
          <li>אפשרות לפרסום</li>
          <li>ניתן לשנות שם</li>
          <li>שימוש בתג פלוס</li>
          <li>תמיכה פלוס</li>
          <li>אפשר לשלוח רק סרטונים שלנו</li>
        </ul>
        <div class="price-tiers">
          <button type="button" class="tier buy-tier" data-item="רצף פלוס (חודש)" data-price="6.22"><span>חודש</span><span>₪6.22</span></button><button type="button" class="subscribe-btn" data-item="רצף פלוס (חודש) (מתחדש)" data-plan="retzef_plus_month" style="width:100%; font-size:11px; padding:6px; margin-top:4px; grid-column:1/-1;">🔁 הרשמה כמנוי</button>
          <button type="button" class="tier buy-tier" data-item="רצף פלוס (שנה)" data-price="9"><span>שנה</span><span>₪9</span></button><button type="button" class="subscribe-btn" data-item="רצף פלוס (שנה) (מתחדש)" data-plan="retzef_plus_year" style="width:100%; font-size:11px; padding:6px; margin-top:4px; grid-column:1/-1;">🔁 הרשמה כמנוי</button>
          <button type="button" class="tier buy-tier" data-item="רצף פלוס (שבוע)" data-price="2.48"><span>שבוע</span><span>₪2.48</span></button><button type="button" class="subscribe-btn" data-item="רצף פלוס (שבוע) (מתחדש)" data-plan="retzef_plus_week" style="width:100%; font-size:11px; padding:6px; margin-top:4px; grid-column:1/-1;">🔁 הרשמה כמנוי</button>
          <button type="button" class="tier buy-tier" data-item="רצף פלוס (3 ימים)" data-price="1.48"><span>3 ימים</span><span>₪1.48</span></button><button type="button" class="subscribe-btn" data-item="רצף פלוס (3 ימים) (מתחדש)" data-plan="retzef_plus_day" style="width:100%; font-size:11px; padding:6px; margin-top:4px; grid-column:1/-1;">🔁 הרשמה כמנוי</button>
          <div class="tier"><span>יום</span><span>חינם (באישור בלבד)</span></div>
        </div>
        <button type="button" class="buy-btn" data-item="רצף פלוס (חודש)" data-price="6.22" style="margin-top:14px;">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="רצף פלוס (חודש)" data-price="6.22" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>

      <div class="plan glass highlight">
        <span class="tag">פרימיום</span>
        <div class="name">רצף פלוס פרימיום 🔥</div>
        <ul>
          <li>שני מקומות לספאם</li>
          <li>פרסום בסרטון</li>
          <li>ניתן לשנות שם ותמונת קבוצה</li>
          <li>שימוש בתג פלוס פרימיום</li>
          <li>תמיכה עם מנהל</li>
          <li>אפשר לשלוח כל סרטון</li>
        </ul>
        <div class="price-tiers">
          <button type="button" class="tier buy-tier" data-item="רצף פלוס פרימיום (חודש)" data-price="9"><span>חודש</span><span>₪9</span></button><button type="button" class="subscribe-btn" data-item="רצף פלוס פרימיום (חודש) (מתחדש)" data-plan="retzef_premium_month" style="width:100%; font-size:11px; padding:6px; margin-top:4px; grid-column:1/-1;">🔁 הרשמה כמנוי</button>
          <button type="button" class="tier buy-tier" data-item="רצף פלוס פרימיום (שנה)" data-price="10"><span>שנה</span><span>₪10</span></button><button type="button" class="subscribe-btn" data-item="רצף פלוס פרימיום (שנה) (מתחדש)" data-plan="retzef_premium_year" style="width:100%; font-size:11px; padding:6px; margin-top:4px; grid-column:1/-1;">🔁 הרשמה כמנוי</button>
          <button type="button" class="tier buy-tier" data-item="רצף פלוס פרימיום (שבוע)" data-price="4.97"><span>שבוע</span><span>₪4.97</span></button><button type="button" class="subscribe-btn" data-item="רצף פלוס פרימיום (שבוע) (מתחדש)" data-plan="retzef_premium_week" style="width:100%; font-size:11px; padding:6px; margin-top:4px; grid-column:1/-1;">🔁 הרשמה כמנוי</button>
          <button type="button" class="tier buy-tier" data-item="רצף פלוס פרימיום (3 ימים)" data-price="2.98"><span>3 ימים</span><span>₪2.98</span></button><button type="button" class="subscribe-btn" data-item="רצף פלוס פרימיום (3 ימים) (מתחדש)" data-plan="retzef_premium_day" style="width:100%; font-size:11px; padding:6px; margin-top:4px; grid-column:1/-1;">🔁 הרשמה כמנוי</button>
          <div class="tier"><span>10 שעות</span><span>חינם (באישור בלבד)</span></div>
        </div>
        <button type="button" class="buy-btn" data-item="רצף פלוס פרימיום (חודש)" data-price="9" style="margin-top:14px;">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="רצף פלוס פרימיום (חודש)" data-price="9" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>

      <div class="plan glass">
        <span class="tag">מנהלים בלבד</span>
        <div class="name">מנוי מנהל</div>
        <div class="price">חינם</div>
        <ul>
          <li>שמור לצוות הניהול של רצף</li>
          <li>כל ההרשאות של רצף פלוס פרימיום</li>
        </ul>
      </div>
    </div>
  </section>
  </div>

  <!-- OWNERS & ADMINS -->
  <section id="team" class="tab-panel">
    <span class="eyebrow" data-i18n="team_eyebrow">מי מנהל את רצף</span>
    <h2 class="sec-title" data-i18n="team_title">בעלים ומנהלים</h2>
    <p class="sec-sub">הרשימה מתעדכנת ידנית — פנו לבעלים כדי להוסיף מנהל.</p>

    <div style="display:flex; flex-direction:column; gap:12px;">
      <div class="glass" style="padding:18px 20px; display:flex; align-items:center; gap:14px;">
        <span style="font-size:26px;">👑</span>
        <div>
          <div style="font-weight:700; font-size:15px;">הבאן המקורי</div>
          <div style="color:var(--muted); font-size:13px;">בעלים</div>
        </div>
      </div>
      <div class="glass" style="padding:18px 20px; display:flex; align-items:center; gap:14px;">
        <span style="font-size:26px;">🛡️</span>
        <div>
          <div style="font-weight:700; font-size:15px;">shirel</div>
          <div style="color:var(--muted); font-size:13px;">מנהלת · הנחת מנהלים / מנוי חינם</div>
        </div>
      </div>
    </div>
  </section>

  <!-- AGE / SCOPE -->
  <section id="scope" class="tab-panel">
    <span class="eyebrow" data-i18n="scope_eyebrow">מי יכול להצטרף</span>
    <h2 class="sec-title" data-i18n="scope_title">גיל ותנאים</h2>
    <div class="notice-grid">
      <div class="notice glass">
        <div class="k">15-</div>
        <div class="v">גיל הכניסה הנוכחי לקבוצה</div>
      </div>
      <div class="notice glass">
        <div class="k">16- בשנת 2027</div>
        <div class="v">הגיל עולה כל שנה</div>
      </div>
    </div>
  </section>

  <!-- SHOP -->
  <section id="shop" class="tab-panel">
    <span class="eyebrow" data-i18n="shop_eyebrow">חנות רצף</span>
    <h2 class="sec-title" data-i18n="shop_title">מה אפשר לקנות</h2>
    <p class="sec-sub" data-i18n="shop_sub">רכישות דרך הבעלים/מנהלים. חלק מהפריטים מסוכנים או מוגבלים — שימו לב להערות.</p>

    <div class="shop-grid">
      <div class="shop-item glass">
        <span class="name">🏷️ תג תורם</span>
        <span class="price">₪0.5</span>
        <button type="button" class="buy-btn" data-item="תג תורם" data-price="0.5">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="תג תורם" data-price="0.5" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
      <div class="shop-item glass">
        <span class="name">💎 תג עשיר</span>
        <span class="price">₪5</span>
        <button type="button" class="buy-btn" data-item="תג עשיר" data-price="5">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="תג עשיר" data-price="5" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
      <div class="shop-item glass">
        <span class="name">📢 לקבל ערעור</span>
        <span class="price">₪2.5</span>
        <button type="button" class="buy-btn" data-item="לקבל ערעור" data-price="2.5">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="לקבל ערעור" data-price="2.5" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
      <div class="shop-item glass">
        <span class="name">🛡️ תג מנהל</span>
        <span class="price free">חינם</span>
        <span class="note">מנהל בלבד</span>
      </div>
      <div class="shop-item glass">
        <span class="name">✏️ שינוי שם/תמונת קבוצה</span>
        <span class="price">₪5</span>
        <span class="note">חד פעמי</span>
        <button type="button" class="buy-btn" data-item="שינוי שם/תמונת קבוצה" data-price="5">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="שינוי שם/תמונת קבוצה" data-price="5" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
      <div class="shop-item glass">
        <span class="name">🗑️ מחיקת הקבוצה</span>
        <span class="price">₪500</span>
        <button type="button" class="buy-btn" data-item="מחיקת הקבוצה" data-price="500">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="מחיקת הקבוצה" data-price="500" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
      <div class="shop-item glass">
        <span class="name">👢 הקיק אנשים</span>
        <span class="price">₪250</span>
        <button type="button" class="buy-btn" data-item="הקיק אנשים" data-price="250">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="הקיק אנשים" data-price="250" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
      <div class="shop-item glass">
        <span class="name">👑 להיות בעלים חדש</span>
        <span class="price">₪350</span>
        <button type="button" class="buy-btn" data-item="להיות בעלים חדש" data-price="350">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="להיות בעלים חדש" data-price="350" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
      <div class="shop-item glass">
        <span class="name">🎲 להיות מנהל בלי ניסיון</span>
        <span class="price">₪25</span>
        <span class="note warn">⚠️ יכול לגרום לחסימה</span>
        <button type="button" class="buy-btn" data-item="להיות מנהל בלי ניסיון" data-price="25">🛒 קנה</button>
        <button type="button" class="add-to-cart-btn" data-item="להיות מנהל בלי ניסיון" data-price="25" style="margin-top:6px; width:100%; justify-content:center; font-size:12.5px; padding:8px;">➕ הוסף לעגלה</button>
      </div>
    </div>

    <div class="settings-card glass" style="margin-top:20px;">
      <h3>🧮 מחשבון מחיר</h3>
      <div style="display:flex; flex-direction:column; gap:12px;">
        <select id="tsCalcItem" style="padding:10px; border-radius:10px; border:1px solid var(--glass-border); background:var(--bg); color:var(--text); font-family:'Assistant';">
          <option value="1">תג תורם — ₪0.5</option>
          <option value="10">תג עשיר — ₪5</option>
          <option value="5">לקבל ערעור — ₪2.5</option>
          <option value="10">שינוי שם/תמונת קבוצה — ₪5</option>
          <option value="1000">מחיקת הקבוצה — ₪500</option>
          <option value="500">הקיק אנשים — ₪250</option>
          <option value="700">להיות בעלים חדש — ₪350</option>
          <option value="50">להיות מנהל בלי ניסיון — ₪25</option>
        </select>
        <div class="toggle-row">
          <span style="font-size:14px; color:var(--muted);">אני מנהל (הנחת 1₪)</span>
          <input type="checkbox" id="tsCalcAdmin" style="width:20px; height:20px; accent-color:var(--accent);">
        </div>
        <div style="font-family:'Rubik'; font-weight:900; font-size:22px; color:var(--accent);" id="tsCalcResult">—</div>
      </div>
    </div>
  </section>

  <!-- SETTINGS -->
  <section id="settings" class="tab-panel">
    <span class="eyebrow" data-i18n="settings_eyebrow">התאמה אישית</span>
    <h2 class="sec-title" data-i18n="settings_title">הגדרות</h2>
    <p class="sec-sub">שנה את המראה של האתר — הצבעים, מצב כהה/בהיר, ועוצמת אפקט הזכוכית.</p>

    <div class="settings-card glass">
      <h3>🔍 אבחון מערכת</h3>
      <p style="font-size:12.5px; color:var(--muted); margin-bottom:10px;">בודק חיבור, PayPal והשרת — שימושי אם משהו לא עובד.</p>
      <button type="button" id="systemDiagnoseBtn" class="btn btn-ghost glass" style="width:100%; justify-content:center;">🔍 הרץ אבחון</button>
      <div id="systemDiagnoseResult" style="margin-top:10px; font-size:12.5px; color:var(--muted); text-align:right; white-space:pre-line;"></div>
      <p style="font-size:12px; color:#b91c1c; margin-top:10px;">⚠️ אם יש שגיאה בטעינה או בתשלום — נסו לכבות VPN או חוסם פרסומות בהגדרות הדפדפן/מכשיר, ולנסות שוב.</p>
    </div>

    <div class="settings-card glass">
      <h3>🌗 מצב תצוגה</h3>
      <div class="toggle-row">
        <span style="font-size:14px; color:var(--muted);">כהה / בהיר</span>
        <button type="button" class="toggle-switch" id="darkModeToggle"></button>
      </div>
    </div>

    <div class="settings-card glass">
      <h3>🎨 צבע ראשי</h3>
      <div class="swatch-row" id="colorSwatches">
        <div class="swatch" data-a="#0A84FF" data-b="#0A84FF" style="background:#0A84FF;" title="iOS Liquid Glass (ברירת מחדל)"></div>
        <div class="swatch active" data-a="#3b63e0" data-b="#7c3aed" style="background:linear-gradient(135deg,#3b63e0,#7c3aed);"></div>
        <div class="swatch" data-a="#e0439b" data-b="#f97316" style="background:linear-gradient(135deg,#e0439b,#f97316);"></div>
        <div class="swatch" data-a="#15803d" data-b="#22c55e" style="background:linear-gradient(135deg,#15803d,#22c55e);"></div>
        <div class="swatch" data-a="#0891b2" data-b="#22d3ee" style="background:linear-gradient(135deg,#0891b2,#22d3ee);"></div>
        <div class="swatch" data-a="#b91c1c" data-b="#f97316" style="background:linear-gradient(135deg,#b91c1c,#f97316);"></div>
        <div class="swatch" data-a="#7c1230" data-b="#ef4444" style="background:linear-gradient(135deg,#7c1230,#ef4444);"></div>
      </div>
    </div>

    <div class="settings-card glass">
      <h3>🖌️ צבעים ספציפיים</h3>
      <div style="display:flex; flex-direction:column; gap:14px;">
        <div class="toggle-row">
          <span style="font-size:14px; color:var(--muted);">צבע רקע</span>
          <input type="color" id="bgColorPicker" value="#f3f4f9" style="width:44px; height:32px; border:none; border-radius:8px; background:none; cursor:pointer;">
        </div>
        <div class="toggle-row">
          <span style="font-size:14px; color:var(--muted);">צבע כרטיסי זכוכית</span>
          <input type="color" id="glassColorPicker" value="#ffffff" style="width:44px; height:32px; border:none; border-radius:8px; background:none; cursor:pointer;">
        </div>
        <div class="toggle-row">
          <span style="font-size:14px; color:var(--muted);">צבע טקסט</span>
          <input type="color" id="textColorPicker" value="#161a26" style="width:44px; height:32px; border:none; border-radius:8px; background:none; cursor:pointer;">
        </div>
        <div class="toggle-row">
          <span style="font-size:14px; color:var(--muted);">צבע כפתור ראשי</span>
          <input type="color" id="btnColorPicker" value="#3b63e0" style="width:44px; height:32px; border:none; border-radius:8px; background:none; cursor:pointer;">
        </div>
      </div>

    <div class="settings-card glass">
      <h3>🧊 עוצמת אפקט הזכוכית</h3>
      <input type="range" id="blurSlider" class="blur-slider" min="0" max="30" value="18">
    </div>

    <div class="settings-card glass">
      <h3>🔤 גודל טקסט</h3>
      <input type="range" id="fontSizeSlider" class="blur-slider" min="85" max="130" value="100">
    </div>

    <div class="settings-card glass">
      <h3>🌐 שפה / Language</h3>
      <div class="lang-row">
        <button type="button" class="lang-btn active" id="langHe">עברית</button>
        <button type="button" class="lang-btn" id="langEn">English</button>
      </div>
      <p style="font-size:11.5px; color:var(--muted); margin-top:10px;">כותרות וניווט מתורגמים; תוכן מפורט נשאר בעברית.</p>
    </div>

    <button type="button" id="resetSettingsBtn" class="btn btn-ghost glass" style="width:100%; justify-content:center;">↺ איפוס לברירת מחדל</button>
  </section>

  <!-- ABOUT & VERSION -->
  <section id="about" class="tab-panel">
    <span class="eyebrow" data-i18n="about_eyebrow">מידע כללי</span>
    <h2 class="sec-title" data-i18n="about_title">אודות וגרסה</h2>
    <p class="sec-sub" data-i18n="about_sub">כל מה שקשור לאתר עצמו — לא לקבוצה.</p>

    <div class="settings-card glass">
      <h3>ℹ️ על האתר</h3>
      <p style="font-size:13.5px; color:var(--muted); line-height:1.7;">
        בית התמיכה הרשמי של קבוצת רצף — חוקים, סטטוס, מנויים, חנות ותשלומים, במקום אחד.
        האתר בנוי כקובץ HTML אחד, ורץ דרך Vercel.
      </p>
    </div>

    <div class="settings-card glass">
      <h3>🧩 גרסה ורכיבים</h3>
      <div style="display:flex; flex-direction:column; gap:10px; font-size:13.5px;">
        <div style="display:flex; justify-content:space-between;"><span style="color:var(--muted);">גרסת אתר</span><span style="font-weight:700;">גישה מקודמת</span></div>
        <div style="display:flex; justify-content:space-between;"><span style="color:var(--muted);">אחסון</span><span style="font-weight:700;">Vercel + GitHub</span></div>
        <div style="display:flex; justify-content:space-between;"><span style="color:var(--muted);">תשלומים</span><span style="font-weight:700;">PayPal · Apple Pay · Google Pay</span></div>
        <div style="display:flex; justify-content:space-between;"><span style="color:var(--muted);">התחברות</span><span style="font-weight:700;">TikTok Login Kit</span></div>
        <div style="display:flex; justify-content:space-between;"><span style="color:var(--muted);">עוזר</span><span style="font-weight:700;">מקומי, חינמי</span></div>
      </div>
    </div>

    <div class="settings-card glass">
      <h3>📝 יומן שינויים</h3>
      <ul style="font-size:12.5px; color:var(--muted); display:flex; flex-direction:column; gap:6px; padding-inline-start:18px;">
        <li>הוספת עוזר רצף (מקומי, חינמי)</li>
        <li>הוספת PWA — התקנה למסך הבית</li>
        <li>הוספת חנות ומחשבון מחיר</li>
        <li>הוספת התחברות TikTok + תשלומי PayPal</li>
        <li>הוספת טאבים, הגדרות עיצוב, ותמיכה דו-לשונית</li>
      </ul>
    </div>
  </section>

  <!-- CONTACT -->
  <footer id="contact" class="tab-panel">
    <div class="contact-card glass">
      <h2 data-i18n="contact_title">צריך עזרה?</h2>
      <p data-i18n="contact_sub">לשאלות על סטטוס, מנויים או חוקים — פנו ישירות.</p>
      <div class="cta-row">
        <a href="https://wa.me/972546020998" target="_blank" class="btn btn-primary">
          <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 1.75.46 3.45 1.32 4.95L2.05 22l5.25-1.38c1.45.79 3.08 1.21 4.74 1.21h.01c5.46 0 9.91-4.45 9.91-9.91 0-2.65-1.03-5.14-2.9-7.01A9.87 9.87 0 0 0 12.04 2z"/></svg>
          וואטסאפ
        </a>
        <a href="mailto:helpme9284772@gmail.com" class="btn btn-ghost glass">אימייל</a>
        <a href="#" id="tiktokGroupLink" class="btn btn-ghost glass">עמוד הקבוצה ב-TikTok</a>
      </div>
    </div>
    <p class="fine">רצף · בית תמיכה — עדכן קישורים, מחיר מנוי הפלוס ופרטי יצירת קשר לפני פרסום.</p>
  </footer>


</div>

<button type="button" class="ai-fab" id="aiFab" title="עוזר AI">🤖</button>

<div class="ai-panel" id="aiPanel">
  <div class="ai-panel-header">
    <span>עוזר רצף</span>
    <button type="button" id="aiClose">✕</button>
  </div>
  <div class="ai-messages" id="aiMessages">
    <div class="ai-msg bot">שאלו אותי על חוקי הקבוצה, סטטוס, מנויים, חנות או ערעורים — עוזר מקומי חינמי (בלי חיבור ל-AI חיצוני).</div>
  </div>
  <div class="ai-input-row">
    <input type="text" id="aiInput" placeholder="שאלה...">
    <button type="button" id="aiSend">שלח</button>
  </div>
</div>

<button type="button" class="cart-fab" id="cartFab" title="עגלת קניות">
  🛒
  <span class="cart-badge" id="cartBadge">0</span>
</button>

<div class="cart-panel" id="cartPanel">
  <div class="cart-panel-header">
    <span>עגלת קניות</span>
    <button type="button" id="cartClose">✕</button>
  </div>
  <div class="cart-items" id="cartItems">
    <div class="cart-empty" id="cartEmptyMsg">העגלה ריקה — לחצו "➕ הוסף לעגלה" על פריטים.</div>
  </div>
  <div class="cart-footer" id="cartFooter" style="display:none;">
    <div class="cart-total"><span>סה"כ</span><span id="cartTotal">₪0</span></div>
    <button type="button" class="btn btn-primary" id="cartCheckoutBtn" style="width:100%; justify-content:center;">💳 לתשלום</button>
  </div>
</div>

<!-- PayPal SDK — עדכנו client-id לפני פרסום. currency=ILS; אם PayPal לא תומך ב-₪ בחשבון שלכם, שנו ל-USD -->
<script src="https://www.paypal.com/sdk/js?client-id=AVXjZhqGVNC8WS5GU0Y6_O_BmkGLP6LV6HcYKZHW5XFAvTOOBe9Pfoywe92-arGXOY9t-ng5X2943zfY&currency=ILS"
  onload="window.__paypalSdkLoaded = true;"
  onerror="window.__paypalSdkFailed = true; console.error('PayPal SDK failed to load — check network connection or an ad/privacy blocker.');">
</script>

<script>
  // === TikTok config — real OAuth, server confirmed working ===
  const TIKTOK_CLIENT_KEY = "awt4ywx6dk6j3wem";
  const TIKTOK_GROUP_URL = "https://tiktok.me/group/ZSfhTkvp4/";
  const TIKTOK_REDIRECT_URI = "https://chi-liart-74.vercel.app/api/callback";

  document.getElementById('tiktokGroupLink').href = TIKTOK_GROUP_URL;

  document.getElementById('tiktokLoginBtn').addEventListener('click', openConsentModal);
  document.getElementById('profileAvatar').addEventListener('click', openConsentModal);

  const consentModal = document.getElementById('consentModal');
  function openConsentModal(){ consentModal.classList.add('open'); }
  function closeConsentModal(){ consentModal.classList.remove('open'); }

  document.getElementById('consentYes').addEventListener('click', function(){
    closeConsentModal();
    const authUrl = `https://www.tiktok.com/v2/auth/authorize/?client_key=${TIKTOK_CLIENT_KEY}&scope=user.info.basic&response_type=code&redirect_uri=${encodeURIComponent(TIKTOK_REDIRECT_URI)}&state=retzef`;
    window.location.href = authUrl;
  });
  document.getElementById('consentNo').addEventListener('click', closeConsentModal);
  consentModal.addEventListener('click', function(e){ if (e.target === consentModal) closeConsentModal(); });

  // After the server redirects back here, reflect the login state and clean the URL.
  (function showLoginResult(){
    const params = new URLSearchParams(window.location.search);
    const statusEl = document.getElementById('tiktokAuthStatus');
    const avatarWrap = document.getElementById('profileAvatar');
    const avatarImg = document.getElementById('profileAvatarImg');
    if (params.get('tiktok_ok') === '1') {
      const username = params.get('username') || '';
      const avatarUrl = params.get('avatar') || '';
      const role = params.get('role') || 'member';
      const roleLabel = role === 'owner' ? 'בעלים' : role === 'admin' ? 'מנהל' : 'חבר';
      statusEl.textContent = username ? `מחובר בתור @${username} · ${roleLabel} ✅` : 'התחברות בוצעה בהצלחה ✅';
      statusEl.style.display = 'block';
      if (avatarUrl) {
        avatarImg.src = avatarUrl;
        avatarWrap.classList.add('logged-in');
        avatarWrap.title = username ? `@${username} · ${roleLabel}` : 'מחובר';
      }
    } else if (params.get('tiktok_error')) {
      const statusEl2 = document.getElementById('tiktokAuthStatus');
      statusEl2.textContent = 'ההתחברות נכשלה (' + params.get('tiktok_error') + '), נסו שוב.';
      statusEl2.style.display = 'block';
    }
    if (params.has('tiktok_ok') || params.has('tiktok_error') || params.has('username') || params.has('avatar') || params.has('role')) {
      params.delete('tiktok_ok'); params.delete('tiktok_error'); params.delete('username'); params.delete('avatar'); params.delete('role');
      const clean = window.location.pathname + (params.toString() ? '?' + params.toString() : '');
      window.history.replaceState({}, '', clean);
    }
  })();

  // === username login gate — stored per-device in localStorage ===
  const PREMIUM_NAMES = ['shirel', 'שירל']; // מי שהשם שלו מופיע כאן מקבל תג "פרימיום" ליד השם
  const usernameModal = document.getElementById('usernameModal');
  const userGreeting = document.getElementById('userGreeting');

  function showGreeting(name){
    const isPremium = PREMIUM_NAMES.some(n => n.toLowerCase() === name.toLowerCase());
    userGreeting.innerHTML = `שלום, ${name}${isPremium ? ' <span style="color:var(--accent); font-weight:700;">✨ פרימיום</span>' : ''}`;
    userGreeting.style.display = 'block';
  }

  const savedUsername = localStorage.getItem('retzef_username');
  if (savedUsername) {
    showGreeting(savedUsername);
  } else {
    usernameModal.classList.add('open');
  }

  document.getElementById('usernameSubmit').addEventListener('click', function(){
    const val = document.getElementById('usernameInput').value.trim();
    if (!val) return;
    localStorage.setItem('retzef_username', val);
    showGreeting(val);
    usernameModal.classList.remove('open');
  });
  document.getElementById('usernameInput').addEventListener('keydown', function(e){
    if (e.key === 'Enter') document.getElementById('usernameSubmit').click();
  });

  // === day streak — anchored so it always increases by 1 per calendar day ===
  // Anchor date is set so that on 2026-08-26 the count reads 335. No manual
  // updates needed — this recalculates from today's date every time the page loads.
  (function updateStreak(){
    const START_DATE = new Date(2025, 8, 25); // 25 בספטמבר 2025 (חודשים ב-JS מתחילים מ-0, אז 8 = ספטמבר)
    const today = new Date();
    const startMidnight = new Date(START_DATE.getFullYear(), START_DATE.getMonth(), START_DATE.getDate());
    const todayMidnight = new Date(today.getFullYear(), today.getMonth(), today.getDate());
    const dayCount = Math.round((todayMidnight - startMidnight) / 86400000);
    const el = document.getElementById('streakDays');
    if (el) el.textContent = dayCount;
  })();

  // === AI assistant widget ===
  const aiFab = document.getElementById('aiFab');
  const aiPanel = document.getElementById('aiPanel');
  const aiMessages = document.getElementById('aiMessages');
  const aiInput = document.getElementById('aiInput');
  const aiSend = document.getElementById('aiSend');

  aiFab.addEventListener('click', () => aiPanel.classList.toggle('open'));
  document.getElementById('aiClose').addEventListener('click', () => aiPanel.classList.remove('open'));

  const AI_CONTEXT = `אתה עוזר AI באתר הרשמי של קבוצת TikTok בשם "רצף". יש לקבוצה אתר תמיכה משלה (האתר הזה שבו אתה נמצא), עם טאבים לסטטוס, חוקים, מנויים, בעלים ומנהלים, חנות, הגדרות, גיל ותנאים, ויצירת קשר — אפשר להפנות לשם כשרלוונטי. בטאב "הגדרות" אפשר לשנות מצב כהה/בהיר, צבע ראשי של האתר, ועוצמת אפקט הזכוכית. הבעלים של הקבוצה נקרא "הבאן המקורי", ו-shirel היא מנהלת. בראש העמוד מוצג מונה "רצף: X ימים" שעולה אוטומטית באחד כל יום.
בטאב "חנות" אפשר לקנות (ולשלם ישירות דרך PayPal באתר): תג תורם (₪0.5), תג עשיר (₪5), לקבל ערעור (₪2.5), תג מנהל (חינם, מנהל בלבד), שינוי שם/תמונת קבוצה (₪5 חד פעמי), מחיקת הקבוצה (₪500), הקיק אנשים (₪250), להיות בעלים חדש (₪350), להיות מנהל בלי ניסיון (₪25 יכול לגרום לחסימה). גם מנויי הסרטון ומנויי רצף פלוס/פרימיום ניתנים לתשלום ישיר דרך PayPal בעמוד.
אם מישהו מספר שקיבל אזהרה/סטטוס/באן ומסביר על מה — אל תסתפק בלהפנות אותו לבעלים ולא תגיד לו "אל תסמוך על ה-AI". תעזור לו בפועל: קודם תבדוק מול שלושת החוקים הרשמיים (ספאם, קללות כלפי היוצר/מנהלים/חברים, שינוי שם/תמונת הקבוצה, ואיסור הזכרת עזה/פלסטין/לבנון/איראן/עיראק) האם הסיבה שהוא מתאר בכלל תואמת חוק קיים. אם היא לא תואמת אף חוק רשמי — תגיד לו את זה בבירור וישירות (למשל: "זו לא סיבה שמופיעה בחוקי הקבוצה"), ועדיין תעזור לו לנסח ערעור קצר וברור שמסביר את זה, לשליחה ל"הבאן המקורי": מה קרה, מי נתן את הסטטוס, מתי, ולמה זה לא תואם אף חוק רשמי. אם הסיבה כן תואמת חוק אמיתי — תסביר את זה בכנות ותעזור לו לנסח ערעור שמביע הבנה/התנצלות ובקשה לבדיקה מחדש, בהתאם למה שהוא בוחר.
אם מישהו מזכיר עצה או תשובה שקיבל מ-AI אחר (כמו Gemini/ChatGPT) לגבי סטטוס בקבוצה — תתייחס לתוכן שהוא מביא לגופו (תבדוק מול החוקים כמו למעלה), ולא תפטור אותו בביטול גורף על "אל תסמוך על AI".
ענה בעברית, קצר וברור.
מידע על הקבוצה:
- מערכת סטטוס: 🟢 הכול טוב, 🟡 אזהרה ראשונה, 🟠 אזהרה שנייה (בסיכון), 🔴 אזהרה חמורה (בסכנה), ⚫ באן, ☠️ חסימה לצמיתות.
- חוקים: 1) לא לספים. 2) לא לקלל את היוצר/מנהלים/חברים. 3) אסור לשנות שם או תמונה של הקבוצה. אסור להזכיר עזה/פלסטין/לבנון/איראן/עיראק.
- החל מ-1.9.2026 שליחת סרטונים דורשת מנוי. יש שתי מערכות מנויים נפרדות:
  (א) "מנוי סרטון" — קובע מה מותר לשלוח בכלל: מנוי סרטון רגיל (2.5₪/חודש, אפשר לשלוח סרטונים שלנו) או מנוי סרטון פלוס (5₪/חודש, אפשר לשלוח לייבים, סרטונים שלנו, וסרטונים של כולם). יש גם לוח הנחות תקופתי למנוי סרטון: 20–25 בינואר חינם, 1 באפריל המחיר עולה בשקל, 1–5 בספטמבר הנחה 50%, 25 בספטמבר הנחה 25%, 1–10 בנובמבר הנחה 11%.
  (ב) מנויי "רצף פלוס" — הטבות קהילה נוספות (לא קשור ישירות להרשאת שליחת סרטונים):
- מנוי "רצף פלוס 🔥": מקום נוסף לספאם, אפשרות לפרסום, ניתן לשנות שם, תג פלוס, תמיכה פלוס, שליחת סרטוני הקבוצה בלבד. מחירים: חודש ₪6.22, שנה ₪9 שבוע ₪2.48, 3 ימים ₪1.48, יום חינם (באישור בלבד).
- מנוי "רצף פלוס פרימיום 🔥": שני מקומות לספאם, פרסום בסרטון, שינוי שם ותמונת קבוצה, תג פלוס פרימיום, תמיכה עם מנהל, שליחת כל סרטון (כולל לייבים וסרטונים אישיים). מחירים: חודש ₪9 שנה ₪10 שבוע ₪4.97, 3 ימים ₪2.98, 10 שעות חינם (באישור בלבד).
- "מנוי מנהל": חינם, שמור למנהלים בלבד, כולל את כל הרשאות רצף פלוס פרימיום.
- מנהלים מקבלים 1₪ הנחה על כל מנוי בתשלום. חברי צוות נבחרים (כמו shirel) מקבלים מנוי בחינם.
- גיל כניסה: 15- כרגע, יעלה ל-16- ב-2027. חשוב: כתבו תמיד "15-" ו-"16-" בדיוק כך (מינוס אחרי המספר) — זה הסימון הרשמי של הקבוצה, גם אם זה נראה לא שגרתי. לעולם אל תכתבו "15+" או "16+".
- קללה כלפי מנהל יכולה להוריד ישר לסיכון/סכנה/באן. אם מישהו מקלל בפרטי אבל הוא בקבוצה, המנהל שנפגע צריך להעביר הוכחות; אם מאומת, המקלל מקבל באן.
אם שואלים משהו שלא קשור לקבוצה, ענה בקצרה ובאדיבות שאתה כאן בעיקר לעזור עם שאלות על רצף.`;

  function localAnswer(question){
    const t = question.toLowerCase();

    if (/ערעור/.test(t)) {
      return 'בשביל ערעור על סטטוס/אזהרה/באן: תכתוב הודעה ל"הבאן המקורי" (הבעלים) שכוללת — מה קרה, מי נתן את הסטטוס, מתי, ולמה לדעתך זו טעות או שאתה מצטער. בדוק קודם מול טאב "חוקים" אם הסיבה בכלל תואמת אחד משלושת החוקים הרשמיים (ספאם, קללות, שינוי שם/תמונה, הזכרת מדינות אסורות) — אם לא, זה שווה לציין בערעור.';
    }
    if (/באן|חסימה|⚫|☠️/.test(t)) {
      return 'סולם הסטטוס: 🟢 הכול טוב → 🟡 אזהרה ראשונה → 🟠 בסיכון → 🔴 בסכנה → ⚫ באן → ☠️ חסימה לצמיתות. פרטים מלאים בטאב "סטטוס", כולל רשימת מי בבאן כרגע.';
    }
    if (/סטטוס|אזהר/.test(t)) {
      return 'הסטטוס עולה לפי חומרת החריגה מהחוקים ולא מתאפס לבד — מנהל מטפל בכל שלב. הרשימה המלאה (🟢🟡🟠🔴⚫☠️) נמצאת בטאב "סטטוס".';
    }
    if (/חוק/.test(t)) {
      return 'שלושת החוקים: 1) לא לספים. 2) לא לקלל את היוצר/מנהלים/חברים. 3) אסור לשנות שם/תמונת הקבוצה. בנוסף אסור להזכיר עזה/פלסטין/לבנון/איראן/עיראק. פירוט מלא בטאב "חוקים".';
    }
    if (/גיל/.test(t)) {
      return 'גיל הכניסה כרגע: 15-. יעלה ל-16- ב-2027.';
    }
    if (/לייב|כולם|סרטונים שלנו|מנוי סרטון/.test(t)) {
      return 'מנוי סרטון רגיל (2.5₪/חודש): רק "אפשר לשלוח סרטונים שלנו". מנוי סרטון פלוס (5₪/חודש): גם לייבים, גם סרטונים שלנו, גם סרטונים של כולם. יש גם לוח הנחות עונתי — בטאב "מנויים".';
    }
    if (/רצף פלוס|פרימיום|ספאם|תג פלוס/.test(t)) {
      return 'רצף פלוס 🔥: מקום נוסף לספאם, פרסום, שינוי שם, תג פלוס, תמיכה פלוס (חודש ₪6.22). רצף פלוס פרימיום 🔥: שני מקומות לספאם, שינוי שם+תמונה, תג פרימיום, תמיכה עם מנהל, כל הסרטונים (חודש ₪9). פירוט מלא בטאב "מנויים".';
    }
    if (/מנוי|מחיר|תשלום|לשלם/.test(t)) {
      return 'יש כמה סוגי מנוי: מנוי סרטון (רגיל/פלוס), רצף פלוס ופרימיום, ומנוי מנהל (חינם, למנהלים בלבד). כל הפרטים והמחירים בטאב "מנויים".';
    }
    if (/חנות|לקנות|תג תורם|תג עשיר|קיק|למחוק/.test(t)) {
      return 'בטאב "חנות" אפשר לקנות: תג תורם (₪0.5), תג עשיר (₪5), ערעור (₪2.5), תג מנהל (חינם, מנהל בלבד), שינוי שם/תמונה (₪5), מחיקת קבוצה (₪500), הקיק (₪250), בעלים חדש (₪350), מנהל בלי ניסיון (₪25 ⚠️ מסוכן).';
    }
    if (/מנהל|בעלים|shirel|שירל/.test(t)) {
      return 'הבעלים: "הבאן המקורי" 👑. shirel היא מנהלת 🛡️. רשימה מעודכנת בטאב "בעלים ומנהלים".';
    }
    if (/רצף\b.*ימים|כמה ימים|סטריק/.test(t)) {
      return 'המונה "🔥 רצף: X ימים" בראש העמוד עולה אוטומטית באחד כל יום.';
    }
    if (/הגדרות|צבע|כהה|בהיר|שפה|english/.test(t)) {
      return 'בטאב "הגדרות" אפשר לשנות מצב כהה/בהיר, צבע ראשי של האתר, עוצמת אפקט הזכוכית, ואפילו את שפת הניווט (עברית/English).';
    }
    return 'אני עוזר מקומי ופשוט (בלי חיבור ל-AI חיצוני) — אני יכול לענות על שאלות לגבי חוקים, סטטוס, מנויים, חנות, בעלים/מנהלים, גיל וערעורים. נסה לשאול על אחד מהנושאים האלה, או עברו לטאב הרלוונטי למעלה.';
  }

  function addMessage(text, cls){
    const div = document.createElement('div');
    div.className = `ai-msg ${cls}`;
    div.textContent = text;
    aiMessages.appendChild(div);
    aiMessages.scrollTop = aiMessages.scrollHeight;
    return div;
  }

  function sendAiMessage(){
    const text = aiInput.value.trim();
    if (!text) return;
    aiInput.value = '';
    addMessage(text, 'user');

    setTimeout(function(){
      const reply = localAnswer(text);
      addMessage(reply, 'bot');
    }, 300);
  }

  aiSend.addEventListener('click', sendAiMessage);
  aiInput.addEventListener('keydown', (e) => { if (e.key === 'Enter') sendAiMessage(); });

  // === banned users — עדכנו את הרשימה הזו ידנית לפי הצורך ===
  const BANNED_USERS = [
    // "username1",
    // "username2",
  ];

  (function renderBannedList(){
    const listEl = document.getElementById('bannedNames');
    const emptyEl = document.getElementById('bannedEmpty');
    if (BANNED_USERS.length === 0) {
      emptyEl.style.display = 'block';
      return;
    }
    BANNED_USERS.forEach(name => {
      const li = document.createElement('li');
      li.textContent = `@${name}`;
      li.style.padding = '8px 12px';
      li.style.borderRadius = '10px';
      li.style.background = 'rgba(239,68,68,.08)';
      li.style.color = '#b91c1c';
      li.style.fontWeight = '600';
      listEl.appendChild(li);
    });
  })();

  document.getElementById('bannedToggleBtn').addEventListener('click', function(){
    const panel = document.getElementById('bannedList');
    panel.style.display = panel.style.display === 'none' ? 'block' : 'none';
  });

  // === language toggle (EN/HE) — headings & nav translated, detailed body text stays Hebrew ===
  const I18N = {
    nav_home: 'Home', nav_status: 'Status', nav_rules: 'Rules', nav_subs: 'Subscriptions', nav_team: 'Owners & Admins',
    nav_scope: 'Age & Terms', nav_shop: 'Shop', nav_settings: 'Settings', nav_about: 'About', nav_contact: 'Contact',
    hero_badge: 'Retzef Community · TikTok', hero_owner_label: 'Owner',
    hero_subtitle: "The group's official support hub — rules, status, subscriptions and login, in one place.",
    hero_streak_label: 'Streak', hero_days_label: 'days',
    hero_login_btn: 'Login with TikTok', hero_tiktok_page_btn: "Retzef's TikTok page", hero_check_status_btn: 'Check status',
    status_eyebrow: 'Status System', status_title: 'Where you stand in the group', status_sub: 'Everyone starts green. Breaking the rules moves you up the ladder — it never resets on its own, an admin handles every step.',
    home_eyebrow: 'Welcome', home_title: 'Home', home_sub: 'Everything you need to know about Retzef, in one place.',
    rules_eyebrow: 'Group Rules', rules_title: 'Rules', rules_sub: 'Three base rules that apply to everyone, at all times.',
    subschange_eyebrow: 'Subscription Update', subschange_title: "What's changing",
    videosubs_eyebrow: 'Video Posting Permission', videosubs_title: 'Video Subscription', videosubs_sub: "Separate from Retzef Plus — this is the subscription that determines what's allowed to be sent at all.",
    plans_eyebrow: 'Community Perks', plans_title: 'Retzef Plus Subscriptions',
    team_eyebrow: 'Who runs Retzef', team_title: 'Owners & Admins',
    scope_eyebrow: 'Who can join', scope_title: 'Age & Terms',
    shop_eyebrow: 'Retzef Shop', shop_title: 'What you can buy', shop_sub: 'Purchases go through the owner/admins. Some items are risky or limited — check the notes.',
    settings_eyebrow: 'Personalization', settings_title: 'Settings',
    about_eyebrow: 'General Info', about_title: 'About & Version', about_sub: 'Everything about the site itself — not the group.',
    contact_title: 'Need help?', contact_sub: 'Questions about status, subscriptions, or rules — reach out directly.',
  };
  const I18N_HE = {}; // captured on first switch to English, so we can restore Hebrew exactly
  let currentLang = 'he';

  function setLanguage(lang){
    const nodes = document.querySelectorAll('[data-i18n]');
    if (lang === 'en') {
      nodes.forEach(el => {
        const key = el.getAttribute('data-i18n');
        if (!(key in I18N_HE)) I18N_HE[key] = el.textContent;
        if (I18N[key]) el.textContent = I18N[key];
      });
      document.documentElement.setAttribute('lang', 'en');
      document.documentElement.setAttribute('dir', 'ltr');
    } else {
      nodes.forEach(el => {
        const key = el.getAttribute('data-i18n');
        if (I18N_HE[key]) el.textContent = I18N_HE[key];
      });
      document.documentElement.setAttribute('lang', 'he');
      document.documentElement.setAttribute('dir', 'rtl');
    }
    currentLang = lang;
    document.getElementById('langHe').classList.toggle('active', lang === 'he');
    document.getElementById('langEn').classList.toggle('active', lang === 'en');
  }
  document.getElementById('langHe').addEventListener('click', () => setLanguage('he'));
  document.getElementById('langEn').addEventListener('click', () => setLanguage('en'));

  // === settings tab ===
  document.getElementById('systemDiagnoseBtn').addEventListener('click', async function(){
    const resultEl = document.getElementById('systemDiagnoseResult');
    resultEl.textContent = 'בודק...';
    const lines = [];

    lines.push('מחובר לאינטרנט: ' + (navigator.onLine ? '✅ כן' : '❌ לא'));
    lines.push('סקריפט PayPal נטען: ' + (window.__paypalSdkLoaded ? '✅ כן' : (window.__paypalSdkFailed ? '❌ נכשל' : '⏳ עדיין לא ידוע')));

    try {
      await fetch('https://www.paypal.com/sdk/js?client-id=test', { mode: 'no-cors', cache: 'no-store' });
      lines.push('חיבור רשת ל-paypal.com: ✅ עבר');
    } catch (err) {
      lines.push('חיבור רשת ל-paypal.com: ❌ נכשל');
    }

    try {
      const res = await fetch(`${PAYPAL_API_BASE_URL}/api/paypal/create-order`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ item: 'בדיקת אבחון', price: 1 }),
      });
      lines.push('שרת PayPal שלנו (Vercel) מגיב: ' + (res.status ? `✅ כן (סטטוס ${res.status})` : '❌ לא'));
    } catch (err) {
      lines.push('שרת PayPal שלנו (Vercel) מגיב: ❌ לא ניתן להגיע אליו');
    }

    try {
      const res = await fetch(`${PAYPAL_API_BASE_URL}/api/callback`, { method: 'GET' });
      lines.push('שרת TikTok שלנו (Vercel) מגיב: ' + (res.status ? `✅ כן (סטטוס ${res.status})` : '❌ לא'));
    } catch (err) {
      lines.push('שרת TikTok שלנו (Vercel) מגיב: ❌ לא ניתן להגיע אליו');
    }

    resultEl.textContent = lines.join('\n');
  });


  const darkToggle = document.getElementById('darkModeToggle');
  darkToggle.addEventListener('click', function(){
    const isDark = document.documentElement.getAttribute('data-theme') === 'dark';
    document.documentElement.setAttribute('data-theme', isDark ? 'light' : 'dark');
    darkToggle.classList.toggle('on', !isDark);
  });

  document.querySelectorAll('.swatch').forEach(sw => {
    sw.addEventListener('click', function(){
      document.documentElement.style.setProperty('--accent', sw.dataset.a);
      document.documentElement.style.setProperty('--accent2', sw.dataset.b);
      document.querySelectorAll('.swatch').forEach(s => s.classList.remove('active'));
      sw.classList.add('active');
    });
  });

  document.getElementById('blurSlider').addEventListener('input', function(e){
    document.documentElement.style.setProperty('--glass-blur', e.target.value + 'px');
  });

  document.getElementById('fontSizeSlider').addEventListener('input', function(e){
    document.body.style.zoom = (e.target.value / 100);
  });

  document.getElementById('bgColorPicker').addEventListener('input', function(e){
    document.documentElement.style.setProperty('--bg', e.target.value);
  });
  document.getElementById('glassColorPicker').addEventListener('input', function(e){
    const hex = e.target.value;
    const r = parseInt(hex.slice(1,3),16), g = parseInt(hex.slice(3,5),16), b = parseInt(hex.slice(5,7),16);
    document.documentElement.style.setProperty('--glass', `rgba(${r},${g},${b},0.55)`);
    document.documentElement.style.setProperty('--bg2', hex);
  });
  document.getElementById('textColorPicker').addEventListener('input', function(e){
    document.documentElement.style.setProperty('--text', e.target.value);
  });
  document.getElementById('btnColorPicker').addEventListener('input', function(e){
    document.documentElement.style.setProperty('--accent', e.target.value);
  });

  const DEFAULTS = {
    theme: 'light',
    accent: '#3b63e0',
    accent2: '#7c3aed',
    bg: '#f3f4f9',
    bg2: '#ffffff',
    glass: 'rgba(255,255,255,0.55)',
    text: '#161a26',
    blur: '18px',
  };
  document.getElementById('resetSettingsBtn').addEventListener('click', function(){
    const root = document.documentElement;
    root.setAttribute('data-theme', DEFAULTS.theme);
    root.style.setProperty('--accent', DEFAULTS.accent);
    root.style.setProperty('--accent2', DEFAULTS.accent2);
    root.style.setProperty('--bg', DEFAULTS.bg);
    root.style.setProperty('--bg2', DEFAULTS.bg2);
    root.style.setProperty('--glass', DEFAULTS.glass);
    root.style.setProperty('--text', DEFAULTS.text);
    root.style.setProperty('--glass-blur', DEFAULTS.blur);

    darkToggle.classList.remove('on');
    document.querySelectorAll('.swatch').forEach(s => s.classList.remove('active'));
    document.querySelector('.swatch[data-a="#3b63e0"]').classList.add('active');
    document.getElementById('blurSlider').value = 18;
    document.getElementById('bgColorPicker').value = DEFAULTS.bg;
    document.getElementById('glassColorPicker').value = DEFAULTS.bg2;
    document.getElementById('textColorPicker').value = DEFAULTS.text;
    document.getElementById('btnColorPicker').value = DEFAULTS.accent;
    document.body.style.zoom = 1;
    document.getElementById('fontSizeSlider').value = 100;
    setLanguage('he');
  });

  // === tab switching ===
  const tabBtns = document.querySelectorAll('.tab-btn');
  const panels = document.querySelectorAll('.tab-panel');
  function activateTab(target){
    panels.forEach(p => p.classList.toggle('active', p.id === target));
    tabBtns.forEach(b => b.classList.toggle('active', b.dataset.target === target));
    window.scrollTo({top: 0, behavior:'smooth'});
  }
  tabBtns.forEach(b => b.addEventListener('click', () => activateTab(b.dataset.target)));
  document.querySelectorAll('.tab-jump').forEach(el => {
    el.addEventListener('click', (e) => { e.preventDefault(); activateTab(el.dataset.target); });
  });


  // reveal ladder rungs on scroll
  const rungs = document.querySelectorAll('.rung');
  rungs.forEach(r => r.style.opacity = 0);
  const io = new IntersectionObserver((entries)=>{
    entries.forEach(en=>{
      if(en.isIntersecting){
        en.target.style.transition = 'opacity .5s ease, transform .5s ease';
        en.target.style.opacity = 1;
        en.target.style.transform = 'translateX(0)';
        io.unobserve(en.target);
      }
    });
  }, {threshold:0.2});
  rungs.forEach(r => { r.style.transform = 'translateX(20px)'; io.observe(r); });

  // === PayPal checkout ===
  // השרת (create-order.js / capture-order.js) רץ על אותו פרויקט Vercel כמו ה-TikTok
  // callback — ראו את התיעוד המלא בהערת ה-HTML בתחתית הקובץ.
  const PAYPAL_API_BASE_URL = "https://chi-liart-74.vercel.app";

  const paypalModal = document.getElementById('paypalModal');
  const paypalItemName = document.getElementById('paypalItemName');
  const paypalItemPrice = document.getElementById('paypalItemPrice');
  const paypalButtonContainer = document.getElementById('paypalButtonContainer');
  const applePayButtonContainer = document.getElementById('applePayButtonContainer');
  const googlePayButtonContainer = document.getElementById('googlePayButtonContainer');

  function openPaypalModal(itemName, price){
    paypalItemName.textContent = itemName;
    paypalItemPrice.textContent = `₪${price}`;
    paypalButtonContainer.innerHTML = '';
    paypalModal.classList.add('open');

    function showManualFallback(reasonHtml){
      paypalButtonContainer.innerHTML = reasonHtml;
    }

    // client-id sanity check — catches an obviously-empty/placeholder id before even
    // trying to render. A truly expired/revoked id can only be confirmed by PayPal's
    // own response when rendering (handled in the catch block below), since validating
    // it for real requires the secret, which never belongs in the browser.
    const clientIdMatch = document.querySelector('script[src*="paypal.com/sdk/js"]');
    const clientIdInUrl = clientIdMatch ? new URL(clientIdMatch.src).searchParams.get('client-id') : null;
    if (!clientIdInUrl || clientIdInUrl === 'YOUR_PAYPAL_CLIENT_ID' || clientIdInUrl.length < 10) {
      console.error('PayPal: client-id looks missing/placeholder in the SDK script tag:', clientIdInUrl);
      showManualFallback('<p style="color:#b91c1c; font-size:13.5px;">ה-client-id של PayPal לא מוגדר כראוי בתג ה-script.</p>');
      return;
    }

    if (typeof paypal === 'undefined') {
      if (window.__paypalSdkFailed) {
        console.error('PayPal SDK script failed to load entirely (network/blocker). client-id used:', clientIdInUrl);
        showManualFallback('<p style="color:#b91c1c; font-size:13.5px;">כפתורי PayPal לא נטענו — לרוב זה חוסם פרסומות/פרטיות בדפדפן שחוסם סקריפטים מ-paypal.com, או בעיית רשת. נסו דפדפן אחר או כבו את החוסם לאתר הזה.</p>');
      } else {
        console.warn('PayPal SDK not yet loaded when button was clicked (window.paypal undefined, no error/load event fired yet). client-id used:', clientIdInUrl);
        showManualFallback('<p style="color:#b91c1c; font-size:13.5px;">PayPal עדיין לא נטען. נסו לרענן את הדף ולנסות שוב בעוד כמה שניות.</p>');
      }
      return;
    }

    try {
      paypal.Buttons({
        style: { layout: 'vertical', shape: 'rect', label: 'pay' },
        createOrder: async function () {
          const res = await fetch(`${PAYPAL_API_BASE_URL}/api/paypal/create-order`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ item: itemName, price: price }),
          });
          const data = await res.json();
          if (!data.id) {
            console.error('PayPal create-order failed. Server response:', data, 'HTTP status:', res.status);
            throw new Error('create-order failed');
          }
          return data.id;
        },
        onApprove: async function (data) {
          const res = await fetch(`${PAYPAL_API_BASE_URL}/api/paypal/capture-order`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ orderID: data.orderID }),
          });
          const details = await res.json();
          if (details.status === 'COMPLETED') {
            paypalButtonContainer.innerHTML = `<p style="color:#15803d; font-weight:700; text-align:center;">✅ התשלום התקבל! תודה על "${itemName}".</p>`;
          } else {
            console.error('PayPal capture did not complete. Server response:', details, 'HTTP status:', res.status);
            paypalButtonContainer.innerHTML = '<p style="color:#b91c1c; text-align:center;">התשלום לא הושלם. נסו שוב.</p>';
          }
        },
        onError: function (err) {
          console.error('PayPal Buttons onError — this usually means an invalid/expired/sandbox-mismatched client-id, or the server endpoint failed:', err);
          showManualFallback('<p style="color:#b91c1c; text-align:center;">שגיאה בתשלום — ייתכן שה-client-id פג תוקף, במצב Sandbox בטעות, או שהשרת לא זמין.</p>');
        },
        onCancel: function () {
          paypalButtonContainer.innerHTML = '<p style="color:var(--muted); text-align:center;">התשלום בוטל.</p>';
        },
      }).render(paypalButtonContainer).catch(function(err){
        console.error('PayPal Buttons failed to render at all (often an invalid client-id):', err);
        showManualFallback('<p style="color:#b91c1c; font-size:13.5px;">כפתורי PayPal לא הצליחו להיטען — כנראה בעיה עם ה-client-id (פג תוקף / Sandbox / לא תקין).</p>');
      });

      // Apple Pay — separate funding source. Only renders on devices/browsers
      // that support Apple Pay AND when it's enabled on the PayPal merchant
      // account (Sandbox/Live) with a verified domain (see setup notes at the
      // bottom of this file). If not eligible, this silently shows nothing —
      // the regular PayPal buttons above still work as the fallback.
      applePayButtonContainer.innerHTML = '';
      if (paypal.isFundingEligible && paypal.isFundingEligible(paypal.FUNDING.APPLEPAY)) {
        paypal.Buttons({
          fundingSource: paypal.FUNDING.APPLEPAY,
          style: { layout: 'vertical', shape: 'rect' },
          createOrder: async function () {
            const res = await fetch(`${PAYPAL_API_BASE_URL}/api/paypal/create-order`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ item: itemName, price: price }),
            });
            const data = await res.json();
            if (!data.id) {
              console.error('Apple Pay create-order failed. Server response:', data, 'HTTP status:', res.status);
              throw new Error('create-order failed');
            }
            return data.id;
          },
          onApprove: async function (data) {
            const res = await fetch(`${PAYPAL_API_BASE_URL}/api/paypal/capture-order`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ orderID: data.orderID }),
            });
            const details = await res.json();
            if (details.status === 'COMPLETED') {
              paypalButtonContainer.innerHTML = `<p style="color:#15803d; font-weight:700; text-align:center;">✅ התשלום התקבל! תודה על "${itemName}".</p>`;
              applePayButtonContainer.innerHTML = '';
            } else {
              console.error('Apple Pay capture did not complete. Server response:', details, 'HTTP status:', res.status);
              paypalButtonContainer.innerHTML = '<p style="color:#b91c1c; text-align:center;">התשלום לא הושלם. נסו שוב.</p>';
            }
          },
          onError: function (err) {
            console.error('Apple Pay button error:', err);
          },
        }).render(applePayButtonContainer).catch(function(err){
          console.warn('Apple Pay button failed to render (device/account not eligible):', err);
        });
      }

      // Google Pay — same pattern as Apple Pay: separate funding source,
      // only renders when the browser/device supports Google Pay AND it's
      // enabled on the PayPal merchant account. Silently shows nothing if
      // not eligible — regular PayPal buttons remain the fallback.
      googlePayButtonContainer.innerHTML = '';
      if (paypal.isFundingEligible && paypal.isFundingEligible(paypal.FUNDING.GOOGLEPAY)) {
        paypal.Buttons({
          fundingSource: paypal.FUNDING.GOOGLEPAY,
          style: { layout: 'vertical', shape: 'rect' },
          createOrder: async function () {
            const res = await fetch(`${PAYPAL_API_BASE_URL}/api/paypal/create-order`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ item: itemName, price: price }),
            });
            const data = await res.json();
            if (!data.id) {
              console.error('Google Pay create-order failed. Server response:', data, 'HTTP status:', res.status);
              throw new Error('create-order failed');
            }
            return data.id;
          },
          onApprove: async function (data) {
            const res = await fetch(`${PAYPAL_API_BASE_URL}/api/paypal/capture-order`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ orderID: data.orderID }),
            });
            const details = await res.json();
            if (details.status === 'COMPLETED') {
              paypalButtonContainer.innerHTML = `<p style="color:#15803d; font-weight:700; text-align:center;">✅ התשלום התקבל! תודה על "${itemName}".</p>`;
              applePayButtonContainer.innerHTML = '';
              googlePayButtonContainer.innerHTML = '';
            } else {
              console.error('Google Pay capture did not complete. Server response:', details, 'HTTP status:', res.status);
              paypalButtonContainer.innerHTML = '<p style="color:#b91c1c; text-align:center;">התשלום לא הושלם. נסו שוב.</p>';
            }
          },
          onError: function (err) {
            console.error('Google Pay button error:', err);
          },
        }).render(googlePayButtonContainer).catch(function(err){
          console.warn('Google Pay button failed to render (device/account not eligible):', err);
        });
      }
    } catch (err) {
      console.error('Unexpected error initializing PayPal Buttons:', err);
      showManualFallback('<p style="color:#b91c1c; font-size:13.5px;">שגיאה בלתי צפויה בטעינת PayPal.</p>');
    }
  }

  // Disable buy buttons gracefully (instead of a broken-looking click) if the SDK
  // never loads within a few seconds of page load.
  window.addEventListener('load', function(){
    setTimeout(function(){
      if (typeof paypal === 'undefined') {
        document.querySelectorAll('.buy-btn, .buy-tier').forEach(btn => {
          btn.style.opacity = '0.5';
          btn.style.cursor = 'not-allowed';
          btn.title = 'PayPal לא זמין כרגע';
        });
      }
    }, 4000);
  });

  document.querySelectorAll('.buy-btn, .buy-tier').forEach(btn => {
    btn.addEventListener('click', () => {
      openPaypalModal(btn.dataset.item, btn.dataset.price);
    });
  });

  // === real recurring subscriptions via PayPal Subscriptions API ===
  // PLAN_IDS must be filled in after running the one-time setup endpoint
  // (api/paypal/create-plans.js — visit it once in a browser, it creates
  // everything on PayPal's side and returns these IDs to paste here).
  // PayPal requires a Plan to exist before any subscribe button can use it —
  // that one visit is unavoidable, it isn't something code alone can skip.
  const PLAN_IDS = {
    video_regular_month: "PASTE_PLAN_ID_HERE",
    video_plus_month: "PASTE_PLAN_ID_HERE",
    retzef_plus_day: "PASTE_PLAN_ID_HERE",
    retzef_plus_week: "PASTE_PLAN_ID_HERE",
    retzef_plus_month: "PASTE_PLAN_ID_HERE",
    retzef_plus_year: "PASTE_PLAN_ID_HERE",
    retzef_premium_day: "PASTE_PLAN_ID_HERE",
    retzef_premium_week: "PASTE_PLAN_ID_HERE",
    retzef_premium_month: "PASTE_PLAN_ID_HERE",
    retzef_premium_year: "PASTE_PLAN_ID_HERE",
  };

  const subModal = document.getElementById('subModal');
  const subItemName = document.getElementById('subItemName');
  const subButtonContainer = document.getElementById('subButtonContainer');

  function openSubscriptionModal(itemName, planKey){
    subItemName.textContent = itemName;
    subButtonContainer.innerHTML = '';
    subModal.classList.add('open');

    const planId = PLAN_IDS[planKey];
    if (!planId || planId === 'PASTE_PLAN_ID_HERE') {
      subButtonContainer.innerHTML = '<p style="color:#b91c1c; font-size:13.5px;">המנוי הזה עדיין לא מוגדר — צריך להריץ את api/paypal/create-plans.js פעם אחת ולהדביק את ה-plan_id שהוא מחזיר בתוך PLAN_IDS בקוד.</p>';
      return;
    }
    if (typeof paypal === 'undefined' || !paypal.Buttons) {
      subButtonContainer.innerHTML = '<p style="color:#b91c1c; font-size:13.5px;">PayPal לא נטען. נסו לרענן, או לכבות VPN/חוסם פרסומות.</p>';
      return;
    }

    try {
      paypal.Buttons({
        style: { layout: 'vertical', shape: 'rect', label: 'subscribe' },
        createSubscription: function (data, actions) {
          return actions.subscription.create({ plan_id: planId });
        },
        onApprove: function (data) {
          // שומר את מזהה המנוי במכשיר הזה בלבד — אין כאן בסיס נתונים,
          // אז זה מה שמאפשר לאתר לבדוק סטטוס בהמשך (ראו checkSubStatusBtn).
          localStorage.setItem('retzef_subscription_id', data.subscriptionID);
          localStorage.setItem('retzef_subscription_name', itemName);
          subButtonContainer.innerHTML = `<p style="color:#15803d; font-weight:700; text-align:center;">✅ נרשמת בהצלחה ל-"${itemName}"! החיוב הבא יתבצע אוטומטית.</p>`;
          updateMySubStatus();
        },
        onError: function (err) {
          console.error('Subscription error:', err);
          subButtonContainer.innerHTML = '<p style="color:#b91c1c; text-align:center;">שגיאה בהרשמה למנוי. נסו שוב.</p>';
        },
        onCancel: function () {
          subButtonContainer.innerHTML = '<p style="color:var(--muted); text-align:center;">ההרשמה בוטלה.</p>';
        },
      }).render(subButtonContainer).catch(function(err){
        console.error('Subscription button failed to render:', err);
        subButtonContainer.innerHTML = '<p style="color:#b91c1c; font-size:13.5px;">כפתור ההרשמה לא נטען. בדקו שה-plan_id תקין.</p>';
      });
    } catch (err) {
      console.error('Unexpected subscription error:', err);
    }
  }

  document.querySelectorAll('.subscribe-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      openSubscriptionModal(btn.dataset.item, btn.dataset.plan);
    });
  });

  document.getElementById('subModalClose').addEventListener('click', () => {
    subModal.classList.remove('open');
  });
  subModal.addEventListener('click', function(e){ if (e.target === subModal) subModal.classList.remove('open'); });

  // status checker — asks PayPal directly (via our server) what the real
  // current status is; this is a poll, not a push, since there's no DB to
  // hold a live webhook-driven status per visitor.
  async function updateMySubStatus(){
    const statusEl = document.getElementById('mySubStatus');
    const subId = localStorage.getItem('retzef_subscription_id');
    const subName = localStorage.getItem('retzef_subscription_name');
    if (!subId) {
      statusEl.textContent = 'לא נמצא מנוי פעיל במכשיר הזה.';
      return;
    }
    statusEl.textContent = `בודק את "${subName}"...`;
    try {
      const res = await fetch(`${PAYPAL_API_BASE_URL}/api/paypal/subscription-status?id=${encodeURIComponent(subId)}`);
      const data = await res.json();
      const statusLabels = {
        ACTIVE: '✅ פעיל',
        CANCELLED: '❌ בוטל',
        SUSPENDED: '⏸️ מושהה (תשלום נכשל)',
        EXPIRED: '⌛ פג תוקף',
        APPROVAL_PENDING: '⏳ ממתין לאישור',
      };
      const label = statusLabels[data.status] || data.status || 'לא ידוע';
      statusEl.textContent = `${subName}: ${label}`;
      if (data.status === 'SUSPENDED' || data.status === 'CANCELLED' || data.status === 'EXPIRED') {
        statusEl.style.color = '#b91c1c';
      } else {
     
